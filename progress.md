# Progress

## Current Focus
**Public face: coming-soon page at `https://korporex.com` and `www.korporex.com`** (since 2026-04-24 afternoon). Apex + www both CNAME to Vercel (`a2768dd48b0e9d48.vercel-dns-017.com`), SSL cert provisioned by Vercel, and the Next.js middleware in [src/middleware.ts](src/middleware.ts) rewrites every non-`/api`, non-`/_next`, non-`/favicon.ico` path to `/soon` for these hostnames. Full WIP site is privately accessible at `https://korporex.vercel.app/*` for continued dev. Email (Cloudflare Email Routing MX + Brevo SPF/DKIM/DMARC) untouched — `contact@korporex.com` works inbound + outbound. **To remove launch mode when ready**: delete `src/middleware.ts`, `git push` — `korporex.com` immediately serves the full site (no DNS change needed).

**Latest session (2026-04-27 — BC removed)**: British Columbia incorporation pulled from the live site to focus operational capacity on Federal + Ontario filings. `Jurisdiction` type narrowed to `"federal" | "ontario"`; BC stripped from every customer-facing surface (hero/soon/contact forms, pricing tabs + cards, services list, footer, incorporation wizard, FAQ enumerations, resource articles, homepage jurisdictions grid, layout metadata, terms/privacy registry mentions, legal-consultation hint). The dedicated "Incorporating in BC" article and the BC section of the annual-returns article were deleted. API Zod enum updated to reject `"bc"`. **Restoration is preserved**: all BC-specific data is archived under [src/archive/bc/](src/archive/bc/) (README with step-by-step restore, full BC article, BC package prices, services entries, pricing-page tab+cards, and verbatim "before" copy snippets for every edited paragraph). `src/archive/` is excluded from the TypeScript build via [tsconfig.json](tsconfig.json) so the archive folder doesn't affect compilation.

### Recently shipped (last few sessions)
- **Lawyer-consultation referral feature** (`/legal-consultation`) — questionnaire → Calendly embed (Hadri Law's `https://calendly.com/hadrilaw/consultation-korporex`, branded with `hide_landing_page_details=1` so the law firm's logo is suppressed) → Stripe Checkout ($150 + 13% HST = $169.50 CAD). `[PENDING]` and `[PAID]` emails go to BOTH `contact@korporex.com` and `contact@hadrilaw.com`. Optional file uploads attached to the email. Disclaimers everywhere — Korporex is not a law firm, consultation is by an independent licensed lawyer, no solicitor-client relationship with Korporex. Entry points: nav tab "Talk to a Lawyer", footer link, services callout, FAQ entry, and a persistent "Not sure? Speak with a lawyer" link below every wizard step.
- **Incorporation wizard now 8 steps** (was 7): Jurisdiction → Package → Business Info → Directors → Shareholders → **Officers (NEW)** → Office Address → Review & Pay.
- **Step 3 — Business Info** additions: required Legal Ending dropdown (9 registry endings), required Official Email Address field, NAICS field renamed to "Primary Activity (NAICS Code)", trimmed hints.
- **Step 4 — Directors** additions: required Country of Tax Residency dropdown (full ISO 3166-1 list, 249 countries via [src/lib/countries.ts](src/lib/countries.ts)), required Citizenship Status radio (Canadian citizen / Permanent resident / Other), CBCA s.2(1) "resident Canadian" description under the existing Canadian-resident checkbox.
- **Step 5 — Shareholders** addition: required Price per Share field (CAD, $-prefixed), with definitional non-advice note explaining `shares × price = subscription total` and a link to `/legal-consultation` for unsure customers.
- **Step 6 — Officers** (NEW): firstName, lastName, position (22 registry titles via [src/lib/officerPositions.ts](src/lib/officerPositions.ts)), email, full address. At least one officer required.
- **Registered office add-on** (Step 7): collapsed from two-tier ($49.99 Mississauga / $99.99 Toronto) to a single tier ($99.99/mo, billed annually $1,199.88, Greater Toronto Area, address chosen by Korporex at filing, **non-refundable**). Sentinel address values flow through to the intake email's `[ACTION REQUIRED]` banner so ops knows to substitute the real GTA address before filing.
- **NUANS / name-search fees** per-jurisdiction: $20 federal, $60 Ontario (was $45 flat).
- **Top nav** picked up a "Talk to a Lawyer" tab between Resources and Contact.
- **Homepage copy tweaks** + About-page copy tweaks (drop trailing "alike", reframe "Why Korporex" headline).

## Outstanding items / next-session backlog
**Strict ship-blockers before flipping launch mode off (`korporex.com` → full site)**:
1. **Legal-drafts review pass** — Terms + Privacy ([src/app/terms/page.tsx](src/app/terms/page.tsx), [src/app/privacy/page.tsx](src/app/privacy/page.tsx)) are structurally complete with the correct entity (Korporex Business Solutions Inc., CBCA, Ontario HQ) and no-refund policy. User-side review still needed for limitation-of-liability cap (CAD $100 floor), governing-law (Ontario), registered-office-service clauses, and any jurisdiction-specific language. Tracked as a `[medium]` known-issue.
2. **Real-card Stripe verification run** on `https://korporex.vercel.app/incorporate` — at least one real-card transaction (smallest order, e.g. Basic Ontario numbered = $399) to confirm the live keys + webhook + `[PENDING]`/`[PAID]` emails all work end-to-end with the new wizard fields (Legal Ending, Official Email, Tax Residency, Citizenship Status, Officers, Price per Share). Previously tested with the test-mode card; the new fields haven't been live-card validated.

**Nice-to-haves / lower priority**:
3. **Vercel webhook URL migration** — Stripe webhook still points at `https://korporex.vercel.app/api/stripe-webhook`. Move to `https://korporex.com/api/stripe-webhook` once the full site is live (cosmetic; webhook works fine at the alias). Tracked as `[low]`.
4. **`NEXT_PUBLIC_GOOGLE_MAPS_API_KEY` in Vercel Preview scope** — set on Production but not Preview. Blocks Google Places autocomplete on any future PR-preview deploys. Vercel CLI v51.8.0 has a `git_branch_required` quirk; dashboard workaround documented in known-issues.md. `[low]`.
5. **Calendly start-time enrichment** — the Calendly `event_scheduled` postMessage doesn't reliably include the slot's start time, so the email currently falls back to "(see Calendly notification)". If lawyers complain, fetch via the Calendly REST API server-side (needs a Calendly Personal Access Token).
6. **Automated tests** — none exist. Existing known-issue.

**State to remember for the next session**:
- The user is the business owner (Korporex Business Solutions Inc.). Hadri Law is the referral lawyer (their email is `contact@hadrilaw.com`).
- Korporex is **not a law firm** — that framing is non-negotiable per [CLAUDE.md](CLAUDE.md). The lawyer-consultation flow is positioned as a referral, not a Korporex service.
- The OneDrive-on-Windows dev environment trips Next.js's `.next` recursive-delete with `EINVAL: readlink` errors. Workaround: `rm -rf .next` from bash before `npm run dev` if it crashes on startup.
- Stripe is **live mode** on Vercel Production. Test transactions = real money. Use `.vercel.app` for testing while DNS is on the coming-soon page (`NEXT_PUBLIC_SITE_URL=https://korporex.vercel.app` is intentional during launch-mode).

## Log

### 2026-04-27 (i18n — site internationalized for English / French / Spanish via next-intl)
- **Stack**: `next-intl` v4 (industry-standard for Next.js 14 App Router). Installed via `npm install next-intl`.
- **Locales**: `en` (default), `fr`, `es`. Defined in [src/i18n/routing.ts](src/i18n/routing.ts) with `localePrefix: "always"` so URLs are symmetric (`/en/...`, `/fr/...`, `/es/...`). Root `/` redirects to `/en/`.
- **App restructure**: every page moved from `src/app/*` to `src/app/[locale]/*`. The root layout is now [src/app/[locale]/layout.tsx](src/app/[locale]/layout.tsx) — there is no `src/app/layout.tsx`. API routes (`src/app/api/*`) stay at the root and are not localized.
- **Middleware** ([src/middleware.ts](src/middleware.ts)): composes `createIntlMiddleware(routing)` (locale routing) with the launch-mode rewrite. The launch-mode rewrite now points to `/<locale>/soon` so visitors stay in their chosen language during the launch hold.
- **Language switcher** ([src/components/layout/LanguageSwitcher.tsx](src/components/layout/LanguageSwitcher.tsx)): top-right of the navbar (and the soon-page header). Globe icon + locale code dropdown showing English / Français / Español. Uses next-intl's locale-aware `useRouter().replace(pathname, { locale })` so the current path is preserved across language switches.
- **Translation files**: `messages/en.json`, `messages/fr.json`, `messages/es.json` — all three carry identical key structure. Initial set covers metadata, common UI, language switcher, nav, footer, soon page, hero/soon contact forms, and the entire homepage (hero, jurisdictions, otherServices, howItWorks, whyUs, testimonials, ctaBottom).
- **Pattern for server pages with `t.rich()`**: split into a server entry (`page.tsx` with `generateMetadata` + `setRequestLocale`) and a client body (`PageNameBody.tsx` with `"use client"`). Required because `t.rich()` callbacks can't serialize across the `NextIntlClientProvider` boundary. Applied to homepage and `/soon`.
- **Locale-aware `Link`**: every page under `[locale]/` switched from `import Link from "next/link"` to `import { Link } from "@/i18n/navigation"`. Bulk-applied via sed across 12 page files. The wizard, legal-consultation, terms, privacy, and other pages keep their existing English text (text not yet extracted) but their internal navigation now respects the active locale.
- **Project CLAUDE.md** updated with the full i18n contract — every future change must use translation keys, locale-aware `Link`, `getTranslations` (server) or `useTranslations` (client), and add new keys to all three messages files in the same edit.
- **Verified**: `npx tsc --noEmit` clean, `npm run lint` clean, `npm run build` green. 36 prerendered pages (12 routes × 3 locales) plus the dynamic article slugs (5 articles × 3 locales = 15 article pages). Middleware bundle 26.6 kB → 38.1 kB (next-intl middleware overhead).
- **Open follow-up** (tracked as `[high]` in [known-issues.md](known-issues.md)): translate the rest of the page content (about, contact, faq, pricing, services, resources, articles, terms, privacy, the 8-step incorporation wizard, legal-consultation, confirmation pages) into FR + ES. The infrastructure is in place — these pages render correctly under all three locale URLs but show English text. Convert each page incrementally as you edit it (the i18n contract in CLAUDE.md says: convert the strings you touch).

### 2026-04-27 (Customer-facing email switched from contact@korporex.com → contact@korporex.ca)
- Per user direction: every customer-facing email reference on the site swapped from `contact@korporex.com` to `contact@korporex.ca`. Affected 16 source files (mailto links, footer, soon page, contact page, legal-consultation flow, error messages, terms, privacy, FAQ, footer, plus four backend `CONTACT_ADDRESS` constants in the API routes and the `LEGAL_CONSULT_RECIPIENTS` entry).
- The two archived BC-removal files (`src/archive/bc/copy.md`, `src/archive/bc/pricing-page.ts`) deliberately retain the old `.com` reference — they're frozen historical records and shouldn't be rewritten.
- The website domain itself (`korporex.com` / `www.korporex.com` in `src/middleware.ts` and the "online platform at korporex.com" wording in legal pages) was **not** changed — only the email address was in scope.
- Verified: `npx tsc --noEmit` clean, `npm run lint` clean.
- **Operational follow-up** (not a code change): the existing Brevo SPF/DKIM/DMARC and Cloudflare Email Routing are configured for `korporex.com`. `contact@korporex.ca` will not receive mail and outbound `from: contact@korporex.ca` will fail SPF unless the same setup is replicated on the `.ca` zone. Logged as a `[high]` known-issue until confirmed bidirectional.

### 2026-04-27 (BC incorporation removed from live site, archived to src/archive/bc/)
- **Decision**: User asked to pull BC incorporation from the website "for now" while keeping it preserved for later restoration. Strategy chosen: narrow the `Jurisdiction` type to `"federal" | "ontario"` (so the TypeScript compiler flags every code path that needs touching) and move BC-specific data + copy into a dedicated `src/archive/bc/` folder that is excluded from the build.
- **Archive folder created** ([src/archive/bc/](src/archive/bc/)):
  - [README.md](src/archive/bc/README.md) — step-by-step restoration procedure
  - [article.ts](src/archive/bc/article.ts) — full "Incorporating in BC" article body, ready to paste back into `src/app/resources/articles.ts`
  - [data.ts](src/archive/bc/data.ts) — BC entries from `pricing.ts` (`PRICES.bc`, `NUANS_FEES.bc`, `JURISDICTION_LABELS.bc`)
  - [services.ts](src/archive/bc/services.ts) — the four BC service entries + BC sole-prop / business-name registration entries from the services page
  - [pricing-page.ts](src/archive/bc/pricing-page.ts) — BC jurisdiction tab + BC pricing-tier cards from the pricing page
  - [copy.md](src/archive/bc/copy.md) — verbatim "before" copy for every paragraph edited (homepage, FAQ, resources articles, layout metadata, terms/privacy, legal-consultation hint, etc.)
- **TypeScript exclude**: added `"src/archive"` to the `exclude` array in [tsconfig.json](tsconfig.json) so the archive doesn't affect compilation.
- **Type narrowing** ([src/lib/pricing.ts](src/lib/pricing.ts)): `Jurisdiction = "federal" | "ontario"`; removed BC entries from `PRICES`, `NUANS_FEES`, `JURISDICTION_LABELS`. `regOfficeAddonAvailable` simplified (BC was the only excluded value). `CA_TAX_RATES.BC` kept since the map is keyed by region code, not jurisdiction (a federal incorporation can still bill to a BC address).
- **API** ([src/app/api/incorporate/route.ts](src/app/api/incorporate/route.ts)): `jurisdiction` Zod enum updated to `["federal", "ontario"]`. `showResidencyRow` simplified from "not Ontario" to "is federal" (BC was the legacy fall-through path).
- **Wizard** ([src/app/incorporate/page.tsx](src/app/incorporate/page.tsx)): BC jurisdiction card removed from Step 1 grid. Step 3 `nameSearchLabel` ternary collapsed (BC was the third branch); `nameSearchApplies` removed since name search now always applies. Step 4 BC-resident checkbox branch deleted (was the legacy fall-through after Ontario was OBCA-Bill-213-fixed). `watch`/`setValue` removed from Step4's destructure (only used by the BC checkbox). Step 7 `regionLock` BC branch removed; `jurisLabel` BC branch removed; the "British Columbia requires a BC registered office" fallback banner deleted.
- **Customer-facing surfaces stripped of BC**:
  - [src/components/HeroContactForm.tsx](src/components/HeroContactForm.tsx) — service `<select>` BC option removed
  - [src/components/SoonContactForm.tsx](src/components/SoonContactForm.tsx) — `SERVICE_OPTIONS` BC entry removed
  - [src/app/soon/page.tsx](src/app/soon/page.tsx) — Incorporation feature sub-line `"Federal · Ontario · BC"` → `"Federal · Ontario"`
  - [src/app/page.tsx](src/app/page.tsx) — `featuredServices` BC card removed; "Choose Your Jurisdiction" copy switched from three jurisdictions to two; grid changed from `md:grid-cols-3` to `md:grid-cols-2`. (Vancouver, BC testimonial kept — testimonials aren't jurisdiction-gated.)
  - [src/app/pricing/page.tsx](src/app/pricing/page.tsx) — page-local `Jurisdiction` type narrowed; BC tab + BC pricing-tier cards removed; registered-office footnote stripped of the BC carve-out sentence
  - [src/app/services/page.tsx](src/app/services/page.tsx) — four BC incorporation entries + BC sole-prop + BC business-name registrations removed
  - [src/components/layout/Footer.tsx](src/components/layout/Footer.tsx) — "BC Incorporation" link removed from Services column
  - [src/app/faq/page.tsx](src/app/faq/page.tsx) — three FAQ answers reworded to drop BC (jurisdiction-difference, type-availability, annual-filings); BC sentence removed from registered-office answer
  - [src/app/resources/articles.ts](src/app/resources/articles.ts) — entire `incorporating-in-bc` article entry deleted; BC section deleted from `corporate-annual-returns-canada`; small BC mentions cleaned up in `incorporation-101` (NUANS coverage row, director-residency row, "BC's BCA" example, "Korporex handles ... BC" claim)
  - [src/app/resources/page.tsx](src/app/resources/page.tsx) — Jurisdiction Comparisons category description updated
  - [src/app/layout.tsx](src/app/layout.tsx) — site description metadata reworded
  - [src/app/terms/page.tsx](src/app/terms/page.tsx) and [src/app/privacy/page.tsx](src/app/privacy/page.tsx) — "BC Corporate Registry" dropped from registry enumerations (three locations)
  - [src/app/legal-consultation/page.tsx](src/app/legal-consultation/page.tsx) — questionnaire jurisdiction hint updated
- **Verified**: `npx tsc --noEmit` clean, `npm run lint` clean, `npm run build` green. Routes 26 → 26 (article slug `/resources/incorporating-in-bc` removed but counted under the dynamic `[slug]` route, so the count is unchanged). `/incorporate` first-load 162 kB → 161 kB; `/incorporate` page-bundle 23.9 kB → 23.4 kB (both consistent with code removal).
- **Restore**: see [src/archive/bc/README.md](src/archive/bc/README.md) for the full procedure. Net effort to put BC back is roughly: re-add `"bc"` to the `Jurisdiction` union, paste the archived data files into the matching maps, paste the BC article back into `articles.ts`, and reverse the `copy.md` snippets in their original files.

### 2026-04-27 (Navbar — bold black tabs + gold hover)
- Top nav tabs were rendering in `text-gray-600` regular weight, which the user flagged as low-contrast against the white navbar. Tried two variants in browser (navy-900 green vs plain black, both at `font-semibold`); user picked black.
- [src/components/layout/Navbar.tsx](src/components/layout/Navbar.tsx) — desktop tabs and mobile drawer entries both updated: `text-gray-600`/`text-gray-700` regular → `font-semibold text-black`, with `hover:text-gold-500` (was `hover:text-navy-900`) so the hover affordance still shows since the default state is now already dark. Same change applied to both menus so they stay consistent.
- Verified live in browser via dev server before saving (per user's "show me the link" workflow).

### 2026-04-27 (Wizard — bold black field labels for visibility)
- Per user direction (screenshot of Step 3 with "CORPORATION NAME TYPE", "PROPOSED CORPORATION NAME", "LEGAL ENDING", "PRIMARY ACTIVITY (NAICS CODE)", "BUSINESS ACTIVITY DESCRIPTION" highlighted in yellow): the wizard's field titles were rendering in `font-semibold` + `text-gray-500`, making them easy to overlook against the white background.
- Single edit in the shared `Field` component ([src/app/incorporate/page.tsx](src/app/incorporate/page.tsx) line 274): `font-semibold` → `font-bold`, `text-gray-500` → `text-black`. Affects every `<Field label=...>` call across all 8 wizard steps (Jurisdiction, Package, Business Info, Directors, Shareholders, Officers, Office Address, Review & Pay) since every step routes its labels through this component.
- Did not touch other forms (contact page, hero, soon page, legal-consult) — user said "the rest of the form" (singular), referring to the incorporation wizard. Other forms can be unified in a follow-up if requested.
- Did not touch non-form headings (Footer column titles, "Order Summary", "Billing Details", confirmation-page metadata) — those are section headings, not field labels.
- Verified: `npx tsc --noEmit` clean, `npm run lint` clean.

### 2026-04-27 (Navbar — remove "Talk to a Lawyer" tab)
- Dropped the `{ href: "/legal-consultation", label: "Talk to a Lawyer" }` entry from the shared `links` array in [src/components/layout/Navbar.tsx](src/components/layout/Navbar.tsx). Removes the tab from both the desktop and mobile nav (both menus iterate the same array).
- All other entry points to `/legal-consultation` are deliberately preserved per user direction: footer "Talk to a Corporate Lawyer" link, services-page callout, FAQ entry, and the persistent "Not sure? Speak with a lawyer" link below every wizard step. The page itself is unchanged.

### 2026-04-27 (Ontario — drop Canadian-resident block from Step 4 + intake email)
- Removed the "Canadian resident" checkbox + paraphrased CBCA s.2(1) paragraph from the Ontario flow on Step 4. OBCA dropped the resident-Canadian director requirement under Bill 213 (in force 2021-07-05), so showing a CBCA-style residency control on an Ontario filing was misleading.
- UI: extended the existing `isFederal ? ... : ...` ternary in [src/app/incorporate/page.tsx](src/app/incorporate/page.tsx) to a three-way branch — federal renders the mandatory radio group + statutory text, **Ontario renders nothing**, BC keeps the legacy checkbox + paragraph. The underlying `Director.isCanadianResident` field still defaults to `"no"` via `seedDir` so the schema validates without a UI.
- Intake email: also dropped the "CBCA resident Canadian" row from Ontario emails ([src/app/api/incorporate/route.ts](src/app/api/incorporate/route.ts)) — gated by `d.jurisdiction !== "ontario"`. Federal + BC emails are unchanged.
- Verified: `npx tsc --noEmit` clean, `npm run lint` clean.

### 2026-04-27 (Federal-only — mandatory Resident Canadian radio + statutory paragraph)
- **Issue**: On Step 4 (Directors), the existing "Canadian resident" checkbox was optional and had a paraphrased description of CBCA s. 2(1) below it. User wanted the federal form to (a) require a conscious yes/no choice rather than allow the checkbox to silently default to unchecked, and (b) show the verbatim statutory definition so customers see the exact legal text they're acknowledging.
- **Scope**: Federal jurisdiction only — Ontario and BC retain the existing checkbox + existing paragraph (untouched per user direction). Step 4's `Step4` component now takes a `jurisdiction: Jurisdiction` prop and branches the render.
- **Federal UI** ([src/app/incorporate/page.tsx](src/app/incorporate/page.tsx)): two-card radio group ("I am a resident Canadian" / "I am not a resident Canadian") styled identically to the Citizenship Status group above it (`has-[:checked]` Tailwind variant). Below the radios, four `<p>` blocks render the statutory text — preamble + clauses (a) (Canadian citizen ordinarily resident in Canada), (b) (citizen not ordinarily resident, prescribed class), (c) (PR ordinarily resident in Canada with the one-year-after-citizenship-eligibility carve-out and the IRPA s.2(1) cross-reference), trailing `(résident canadien)` French statutory marker preserved.
- **Data model**: `Director.isCanadianResident` migrated from `boolean` to `ResidentChoice = "yes" | "no"`. Empty cast (`"" as ResidentChoice`) used at the federal seed so the radio group renders unselected; Zod's `z.enum(["yes", "no"], { message: "Required" })` rejects the empty value at submit time, surfacing a "Required" error if the customer tries to skip. Non-federal jurisdictions seed `"no"` (= unchecked checkbox) so legacy UX works without forced interaction. Same cast pattern already used by `legalEnding` and `citizenshipStatus`.
- **Non-federal preserved**: existing checkbox stays. Wired via `watch`/`setValue` to translate checked/unchecked into `"yes"`/`"no"` strings so the schema sees a valid enum value. The paragraph below it is unchanged from before.
- **API** ([src/app/api/incorporate/route.ts](src/app/api/incorporate/route.ts)): `directorSchema.isCanadianResident` updated to `z.enum(["yes", "no"])`. Intake email's "CBCA resident Canadian" row updated to `x.isCanadianResident === "yes" ? "Yes" : "No"`. No change to email layout or recipients.
- **`Add Another Director`** button now appends a per-jurisdiction seed (federal: empty cast, non-federal: `"no"`) instead of the bare `emptyDir`, so adding a second director on the federal form also lands at a forced-pick state.
- **Verified**: `npx tsc --noEmit` clean, `npm run lint` clean, `npm run build` green (routes unchanged at 26; `/incorporate` 23.9 kB / 162 kB first-load — within ±0.1 kB of prior session). Did not capture a screenshot — no Puppeteer/Playwright in this dev environment, prior screenshots in `build-tmp/` were ad-hoc one-offs. Code changes are well-typed and isomorphic with the existing Citizenship Status radio pattern, so confidence is high without a visual.

### 2026-04-25 (Step 3 copy — drop "Many corporations pick December 31" hint)
- Removed the small grey hint under the Fiscal Year End month/day fields in [src/app/incorporate/page.tsx](src/app/incorporate/page.tsx). Per user direction — keeps the wizard free of suggestive framing on choices the customer should make for themselves (consistent with the earlier no-guidance treatment of Price per Share on Step 5).

### 2026-04-25 (UX — wizard scrolls to top on step change)
- **Issue**: After completing a step in `/incorporate` and clicking Continue, the next step rendered with the page still scrolled to the bottom (where the previous step's CTA was), forcing the customer to scroll up manually to see the new step's heading and first field.
- **Fix**: Added a `useEffect(() => window.scrollTo({ top: 0, behavior: "auto" }), [step])` at the top of `IncorporatePage` in [src/app/incorporate/page.tsx](src/app/incorporate/page.tsx). One hook handles all 8 steps, both forward (Continue) and backward (Back) transitions.
- **Why instant (`behavior: "auto"`) rather than smooth**: Step 4 (Directors) and Step 5 (Shareholders) can be very long when multiple records are added — a smooth scroll over thousands of pixels is disorienting and slow. Instant snap to top mirrors the mental model of "I've moved to a new page" and is the convention used by other multi-step checkout flows.
- **Verified**: `npx tsc --noEmit` clean, `npm run lint` clean.

### 2026-04-24 (new feature — Lawyer-consultation referral flow)
- **Decision (from user)**: Korporex offers a $150 + HST 30-minute consultation with an independent corporate lawyer from a trusted referral network (currently Hadri Law). Korporex is not a law firm and the consultation is not Korporex's service — Korporex's role is referral facilitation only.
- **Flow** (Calendly-first, Pay-second per user direction): Customer fills questionnaire → picks Calendly slot → Stripe Checkout → confirmation page. Calendly slot is reserved BEFORE payment so the customer commits to a specific time before paying. Operationally, an unpaid Calendly booking can be released manually if the customer abandons checkout.
- **New files**:
  - [src/lib/legalConsult.ts](src/lib/legalConsult.ts) — shared constants (topics, incorp-status options, file-upload caps, Calendly URL fallback), Zod schema, recipient list (`contact@korporex.com` + `contact@hadrilaw.com`).
  - [src/app/legal-consultation/page.tsx](src/app/legal-consultation/page.tsx) — staged page with three states (`form` → `calendly` → `ready` → `submitting`/`error`). Uses React Hook Form + Zod. Calendly inline widget loaded via `next/script` (`assets.calendly.com/assets/external/widget.js`); listens for `calendly.event_scheduled` postMessage to capture event/invitee URIs. After slot selection, reveals "Continue to Payment" button which POSTs to `/api/legal-consult`.
  - [src/app/legal-consultation/confirmation/page.tsx](src/app/legal-consultation/confirmation/page.tsx) — post-payment thank-you page; reads Stripe session metadata for the order ref, slot time, and amount.
  - [src/app/api/legal-consult/route.ts](src/app/api/legal-consult/route.ts) — accepts `multipart/form-data` (questionnaire fields + optional file uploads up to 3 files / 5 MB total / PDF·JPG·PNG only). Validates with the shared schema, sends `[PENDING]` intake email to BOTH `contact@korporex.com` and `contact@hadrilaw.com` with file attachments via Brevo, then creates a Stripe Checkout Session ($150 + 13% HST = $169.50 total) tagged with `productType: "legal-consult"` metadata.
- **Updated files**:
  - [src/lib/pricing.ts](src/lib/pricing.ts) — added `LEGAL_CONSULT_FEE = 150`, `LEGAL_CONSULT_TAX_RATE = 0.13` (Ontario HST — place of supply for Ontario-based lawyer), and `getLegalConsultPricing()` helper.
  - [src/app/api/stripe-webhook/route.ts](src/app/api/stripe-webhook/route.ts) — `handleCheckoutCompleted` and `handleCheckoutFailed` now branch on `productType` metadata. New `handleLegalConsultPaid` / `handleLegalConsultFailed` send `[PAID]` / `[PAYMENT FAILED]` emails to BOTH recipients via a new `sendBrevoEmailToMany` helper. Existing incorporation emails continue to use the original `sendBrevoEmail` helper (single-recipient).
  - [src/components/layout/Footer.tsx](src/components/layout/Footer.tsx) — Services column gains "Talk to a Corporate Lawyer" link to `/legal-consultation`. Restored "Dissolve a Business" entry that I'd accidentally swapped out.
  - [src/app/services/page.tsx](src/app/services/page.tsx) — new callout block between the categories list and the bottom CTA: "Need Personalized Legal Advice? Talk to a Trusted Corporate Lawyer" with a Book a Consultation button. Imports `ScaleIcon` from lucide-react.
  - [src/app/faq/page.tsx](src/app/faq/page.tsx) — new FAQ entry under General: "What if I need personalized legal advice?" (links to /legal-consultation). The existing "Is Korporex a law firm?" entry now references the new question. Also fixed the stale registered-office answer that still mentioned the old Basic/Premium tiers — now correctly describes the single $99.99/mo GTA tier with non-refundable language.
- **Disclaimers everywhere**: hero, prominent banner, footer fine-print, confirmation page bottom — all surface that Korporex is not a law firm, the lawyer is independent, no solicitor-client relationship is created with Korporex, and the $150 fee covers only the consultation (further engagement is between customer and lawyer at the lawyer's standard rates).
- **Configurability**: `NEXT_PUBLIC_CALENDLY_LAWYER_URL` env var (placeholder URL `https://calendly.com/placeholder-korporex-lawyer/30min` until user provides Hadri Law's real Calendly link).
- **Verified**: `npx tsc --noEmit` clean, `npm run lint` clean, `npm run build` green. Routes: 24 → **26** (`/legal-consultation` 10.3 kB / 148 kB first-load static; `/legal-consultation/confirmation` and `/api/legal-consult` dynamic). Visual screenshot at [build-tmp/legal-consult-verify/01-questionnaire.png](build-tmp/legal-consult-verify/01-questionnaire.png) confirms the page renders end-to-end with all 10 topic checkboxes, hero, disclaimer, all questionnaire fields, and Continue button. `/services` callout strings ("Need Personalized Legal Advice", "Talk to a Trusted Corporate Lawyer") confirmed in rendered HTML.
- **Open follow-ups**:
  1. The Calendly `event_scheduled` postMessage doesn't reliably include the start time; we currently fall back to "(see Calendly notification)" when absent. If users complain about the friendly time not appearing in the email, we can use the Calendly REST API server-side to fetch the start time given the event URI (Calendly Personal Access Token required).
  2. No automated test for the new flow — same gap as the rest of the wizard. Tracked under the existing "No automated tests" known-issue entry.

### 2026-04-25 (Shareholders — Price per Share + persistent lawyer link in wizard)
**Two changes per user direction:**

1. **New required Price per Share field on Step 5** (Shareholders).
   - `Shareholder.pricePerShare: string`; Zod refines to a positive numeric.
   - Default `1.00` in `emptySH`.
   - UI: text-prefixed currency input (`$` adornment + `pl-8`), labeled `Price per Share (CAD) *`.
   - Non-advice note rendered below the field: "Enter any positive dollar amount per share — the price can be $1.00. The total subscription amount equals price per share × number of shares (for example, 100 shares × $1.00 = $100.00 total). This is a numeric example only. Korporex does not provide legal or tax advice on share pricing — if you're unsure, speak with a corporate lawyer." Phrasing is deliberately definitional/illustrative; avoids any "many corporations choose..." or "typically founders set..." framing per user direction. The "speak with a corporate lawyer" link points to `/legal-consultation`.
   - `/api/incorporate` `shareholderSchema` mirrors. Intake email's shareholder block now shows two new rows: `Price per share` (formatted `$1.00 CAD`) and `Subscription total` (computed `shares × price`, formatted `$100.00 CAD`).

2. **Persistent "Not sure? Speak with a lawyer" link below every wizard step.**
   - Implemented once at the page level in `IncorporatePage` — sits below the step content inside the wizard's `pb-20` container, separated by a top border. Appears across all 8 steps without per-step duplication.
   - Subtle styling (small grey text, only the action phrase underlined) so it doesn't compete with the primary `Continue` / `Continue to Payment` CTAs. Links to `/legal-consultation`.
   - Confirmed via curl that "Not sure? Speak with a lawyer" copy is rendered on Step 1 (default), and visually verified on Step 5 ([build-tmp/step5-shareholders-pps/01-step5.png](build-tmp/step5-shareholders-pps/01-step5.png)).

Build/tsc/lint green.

### 2026-04-24 (Directors — Citizenship Status + CBCA-resident description)
- New required field on each director: **Citizenship Status** — radio group with three mutually-exclusive options (Canadian citizen / Permanent resident / Other). Customer must pick one.
- The existing "Canadian resident" checkbox now has a description below it explaining the CBCA s.2(1) definition: "Per CBCA s.2(1), a 'resident Canadian' is a Canadian citizen who ordinarily lives in Canada, or a permanent resident who ordinarily lives in Canada (and has not been a permanent resident long enough to be eligible for Canadian citizenship). Tick this box only if you meet that definition."
- **Why two fields**: citizenship status and CBCA "resident Canadian" status are orthogonal — the latter requires both Canadian citizenship/PR AND ordinarily living in Canada. Splitting them lets us capture each cleanly. The CBCA description is faithful to s.2(1) including the one-year-after-citizenship-eligibility caveat for permanent residents.
- **Implementation**:
  - New `CitizenshipStatus = "citizen" | "permanent_resident" | "other"` union and a shared `CITIZENSHIP_OPTIONS` const at the top of [src/app/incorporate/page.tsx](src/app/incorporate/page.tsx).
  - `Director.citizenshipStatus: CitizenshipStatus`; `emptyDir.citizenshipStatus: "" as CitizenshipStatus` (cast pattern matches `legalEnding`).
  - Wizard's `directorSchema` Zod uses `z.enum([...], { message: "Select citizenship status" })`; same in the API schema.
  - Step 4 UI: radio group renders as three native `<input type="radio">` elements wrapped in styled labels using Tailwind's `has-[:checked]` variant (Tailwind 3.4+) — semantically correct radios, visually consistent with other selectable cards in the wizard.
  - The previous default of `isCanadianResident: true` flipped to `false` since customers should now consciously pick.
  - Intake email director block adds a new "Citizenship" row (rendered as the friendly label) and renames the prior "Canadian resident" row to "CBCA resident Canadian" so the operator sees both signals separately.
- **Verified**: tsc/lint/build green. Screenshot at [build-tmp/step4-tax-residency/02-citizenship.png](build-tmp/step4-tax-residency/02-citizenship.png) shows the three-up radio group above the Canadian-resident checkbox, with the CBCA description rendered as small grey copy directly underneath.

### 2026-04-24 (Directors — Country of Tax Residency field)
- New required field on each director (Step 4) capturing country of tax residency, per user direction.
- New shared module [src/lib/countries.ts](src/lib/countries.ts) with the full ISO 3166-1 alpha-2 list (249 entries, alphabetized by English short name). Kept separate from the existing 12-entry `COUNTRIES` const used by the address dropdown — that one's intentionally restricted to common countries plus an `Other` fallback; migrating it later is a separate task.
- [src/app/incorporate/page.tsx](src/app/incorporate/page.tsx):
  - `Director` interface gains `taxResidencyCountry: string`.
  - `directorSchema` Zod requires `min(2)` (rejects empty placeholder).
  - `emptyDir` defaults to `""`.
  - Step 4 UI renders a full-width `<select>` between the address subform and the Canadian-resident checkbox. Hint clarifies the field is independent of the mailing address: "The country where the director is a tax resident — may differ from their mailing address."
  - The Canadian-resident checkbox below is preserved — that's residency for Canadian corporate-law purposes (CBCA / OBCA director-residency requirements), not tax. Two separate concepts that should both be captured.
- [src/app/api/incorporate/route.ts](src/app/api/incorporate/route.ts):
  - `directorSchema` Zod adds the field.
  - Intake email director block resolves the ISO code to the country's display name via `ALL_COUNTRIES.find(...)`, formatted as `Canada (CA)` for clarity.
- **Verified**: tsc/lint/build green. Dev-server screenshot at [build-tmp/step4-tax-residency/01-with-tax-residency.png](build-tmp/step4-tax-residency/01-with-tax-residency.png) confirms the new field renders correctly with the placeholder and hint. `curl` spot-check confirms 249 unique country options in the rendered HTML (`Afghanistan` through `Zimbabwe`).

### 2026-04-24 (lawyer-consult — Talk to a Lawyer nav tab)
- User opted to add a regular nav tab ("Option 1" of two suggestions — the other was a small text link beside the Incorporate Now button) rather than a co-equal top-right button. Reasoning: a second prominent CTA would split attention from the primary $399–$999 incorporation funnel and crowd the navbar on mobile; a nav tab keeps the lawyer-referral discoverable but visually subordinate to Incorporate Now.
- [src/components/layout/Navbar.tsx](src/components/layout/Navbar.tsx) — added `{ href: "/legal-consultation", label: "Talk to a Lawyer" }` between Resources and Contact in the shared `links` array. Both desktop (`hidden lg:flex`) and mobile (`open && ...`) menus auto-inherit, so the same entry appears in both. Verified visually at 1400px wide: `KORPOREX | Services Pricing About FAQ Resources Talk to a Lawyer Contact | Incorporate Now` fits comfortably with no overflow ([build-tmp/legal-consult-verify/05-navbar-with-tab.png](build-tmp/legal-consult-verify/05-navbar-with-tab.png)).
- Label decision: picked "Talk to a Lawyer" over "Legal Help" because it's action-oriented and makes clear the customer is talking *to* an external lawyer (consistent with the page heading "Talk to a Trusted Corporate Lawyer" and footer link "Talk to a Corporate Lawyer"). "Legal Help" was ambiguous about whether Korporex was the helper, which conflicts with the not-a-law-firm positioning.

### 2026-04-24 (lawyer-consult — Calendly cutoff + branding leak fix)
- **Issue 1 (cutoff)**: Calendly iframe was rendering with internal scroll arrows because the container was set to `min-h-[700px]` — too short for Calendly's calendar grid + slot picker. The iframe was getting created at `100% × 100%` of the parent and Calendly's content was overflowing inside, producing the up/down arrow scroll controls visible in the screenshot.
- **Issue 2 (branding)**: Calendly was showing the host's logo and "Hadri Law Professional Corporation" name above the event title, exposing the law firm's branding when the customer is supposed to experience the booking as a Korporex flow.
- **Fixes** ([src/app/legal-consultation/page.tsx](src/app/legal-consultation/page.tsx)):
  1. Container changed from `min-h-[700px]` to `h-[1100px]` — fixed height tall enough that Calendly never needs internal scroll. Stage container width also widened from `max-w-2xl` to `max-w-4xl` for the Calendly stage only (form + ready stages still use `max-w-2xl` since that's correct for narrow form / status panels).
  2. Calendly URL gets `hide_landing_page_details=1&hide_gdpr_banner=1` appended at runtime via `URL` API in the `initInlineWidget` call. The first param hides the host's avatar + firm name; the second hides the GDPR banner. The event title (`Consultation - KORPOREX`) stays visible because that's controlled by the event itself, not by `hide_landing_page_details`.
- **Verified**: tsc clean, build green. Dev-server DOM dump confirms the iframe is mounted with the full querystring `https://calendly.com/hadrilaw/consultation-korporex?embed_domain=localhost%3A3000&embed_type=Inline&hide_landing_page_details=1&hide_gdpr_banner=1` and that the parent has `h-[1100px]`. Screenshot at [build-tmp/legal-consult-verify/04-calendly-loaded.png](build-tmp/legal-consult-verify/04-calendly-loaded.png) confirms the structural box is properly sized and outlined; the Calendly iframe content itself doesn't render under headless Chrome's virtual-time budget (real-world async fetch from calendly.com), but it does render in real browsers — verify on `korporex.vercel.app/legal-consultation` after Vercel finishes the auto-deploy.

### 2026-04-24 (lawyer-consult — Calendly URL plumbed in)
- User provided the real Calendly link: `https://calendly.com/hadrilaw/consultation-korporex`.
- Updated [src/lib/legalConsult.ts](src/lib/legalConsult.ts) so the real URL is the default fallback (replacing the earlier placeholder). The `NEXT_PUBLIC_CALENDLY_LAWYER_URL` env var still overrides it, so future swaps to a different referral lawyer or a test event don't need a code change.
- Approach chosen: code default rather than Vercel env var. Reasoning: `NEXT_PUBLIC_*` vars bake into the client bundle at build time anyway, so there's no operational difference; the code-default version also makes local dev "just work" without anyone needing to set up the env var. One push triggers the Vercel redeploy that bakes the real URL into the production bundle.

### 2026-04-24 (new wizard Step 6 — Officers)
- **Decision (from user)**: Officers must be captured between Shareholders and Office Address. Each officer needs: First Name, Last Name, Position (from a fixed dropdown of 22 registry titles), Email Address, Street Address, City, Province, Postal/ZIP, Country. At least one officer is required. The "Other (untitled)" option visible in the source registry list was deliberately excluded — customers must pick a defined position.
- **Wizard structure shifted from 7 steps to 8**:
  - 1 Jurisdiction · 2 Package · 3 Business Info · 4 Directors · 5 Shareholders · **6 Officers (NEW)** · 7 Office Address · 8 Review & Pay
  - `STEP_LABELS` updated; the existing horizontal progress bar auto-rerenders 8 cells and the bottom mobile bar auto-scales `width = current / 8`.
- **New shared module** [src/lib/officerPositions.ts](src/lib/officerPositions.ts): `OFFICER_POSITIONS` const tuple (22 entries, registry order preserved — `Authorized Signing Officer` sits between the two `Assistant…` entries by source-list convention), `OfficerPosition` type, `officerPositionSchema` Zod enum.
- **Wizard (`src/app/incorporate/page.tsx`)**:
  - New `Officer` interface (with `position: OfficerPosition`; the empty "" placeholder is cast at the seed value, same pattern used for `legalEnding`).
  - New `officerSchema` and `s6 = z.object({ officers: z.array(officerSchema).min(1) })`.
  - **Renamed** the previous `s6` (regOffice) → `s7`, previous `s7` (review) → `s8`. Step component renames in lockstep: `Step6` (regOffice) → `Step7`, `Step7` (review) → `Step8`. Internal `useForm<S6>`/`zodResolver(s6)` references updated.
  - **NEW `Step6` component** — modeled on Step4 (Directors). Card-per-officer with `useFieldArray`, "Position" `<select>` populated from `OFFICER_POSITIONS` with a `-- Please Select --` placeholder, email input, address subform via `<AddressFields>`, "+ Add Another Officer" button, single-officer entries can't be removed.
  - `IncorporatePage` step switch updated: step 6 renders the new Officers component (passing `def={{ officers: data.officers }}`); step 7 renders the renumbered RegOffice step; step 8 renders the renumbered Review step. Back/forward `setStep(N)` calls all bumped.
  - `init` extended with `officers: []`; the wizard's `onPay` payload now includes `officers: data.officers`.
  - Step 8 (Review) Order Summary gains a new "Officers" row showing the count.
- **`/api/incorporate`** (`src/app/api/incorporate/route.ts`):
  - New `officerSchema` mirroring the wizard's; `officers: z.array(officerSchema).min(1).max(50)` added to the payload schema.
  - Intake email body includes a new "Officers" section using the existing `personBlock` helper, between Shareholders and Registered Office. Each officer block shows Name / Position / Email / Address.
  - Note copy in the [PENDING] email tweaked: "captured at Step 7 submit" → "captured at Review submit" (so the wording is step-number-agnostic — survives future renumbering).
- **Verified**: `npx tsc --noEmit` clean (after a one-pass type-mismatch fix on `Officer.position` — narrowed from `OfficerPosition | ""` to `OfficerPosition` with a cast at the seed, same pattern used for `legalEnding`), `npm run lint` clean, `npm run build` green. Visual screenshot at [build-tmp/step6-officers-verify/01-officers.png](build-tmp/step6-officers-verify/01-officers.png) confirms: 8-step progress bar with "Officers" highlighted, single Officer 1 card with all required fields, Position dropdown showing "-- Please Select --", "+ Add Another Officer" button, Continue button. `curl` confirms all 22 position values present in the rendered `<option>` list.

### 2026-04-24 (Step 3 — Official Email Address field)
- New required field on Step 3 between Business Activity Description and Fiscal Year End: **Official Email Address**, validated as a real email by Zod (`z.string().email()`).
- Threaded through end-to-end: `WizardData.officialEmail`, `s3` Zod schema, Step 3 defaults, init, Step 3 → IncorporatePage `def` prop, onPay payload, Step 7 review (new "Official Email" row in the Order Summary), and `/api/incorporate` schema + intake email (new "Official email" row in the Order section of the [PENDING] email).
- Hint: "The corporation's primary contact email for government notices and correspondence." Placeholder: `e.g. contact@yourcompany.com`.
- **Verified**: tsc / lint / build all green. Visual screenshot at [build-tmp/step3-verify/03-with-official-email.png](build-tmp/step3-verify/03-with-official-email.png) confirms the field renders between Business Activity and Fiscal Year End with proper styling, placeholder, and hint copy.

### 2026-04-24 (NAICS field copy tweaks)
- Step 3 NAICS field: label `Industry (NAICS Code)` → `Primary Activity (NAICS Code)` so the question reads as a direct request rather than a category bucket.
- Hint trimmed: dropped trailing sentence "This is required for several government registrations." per user direction. Hint now just reads "Search by code, activity, or sector."

### 2026-04-24 (NUANS / name-search fee — per-jurisdiction)
- **Decision (from user)**: Real pass-through fees confirmed: **$20 for federal NUANS, $60 for Ontario name search**. Previously a single `NUANS_FEE = 45` placeholder constant covered both.
- **`src/lib/pricing.ts`**: replaced single constant with `NUANS_FEES: Record<Jurisdiction, number> = { federal: 20, ontario: 60, bc: 0 }`. Added `getNuansFee(jurisdiction)` helper. `computePricing` now calls `getNuansFee(args.jurisdiction)` instead of using the flat constant.
- **Wizard Step 3 callout** (`src/app/incorporate/page.tsx`): the "$45 report fee" line now reads `${getNuansFee(jurisdiction)}` so Federal renders `$20` and Ontario renders `$60`. Import swapped from `NUANS_FEE` → `getNuansFee`. Comment block at the top of the file also updated.
- **`/api/incorporate` Stripe line item**: the line-item label now adapts — `"NUANS name search report"` for federal and `"Ontario name search report"` for Ontario; description includes the jurisdiction label.
- **`known-issues.md`**: closed the previous low-sev "NUANS pass-through fee is a placeholder" entry.
- **Verified**: `npx tsc --noEmit` clean, `npm run lint` clean, `npm run build` green. No bundle size change. Manually re-checked Step 3 callout copy on the Ontario default — `getNuansFee("ontario")` returns 60, so the callout reads "A $60 report fee applies…".

### 2026-04-24 (incorporation wizard — required Legal Ending dropdown on Step 3)
- **Decision (from user)**: Every incorporation must capture a "legal ending" (legal element) from a fixed list, regardless of jurisdiction (federal / Ontario / BC) or corp-name type (named / numbered). Options: `CORP.`, `CORPORATION`, `INC.`, `INCORPORATED`, `INCORPORÉE`, `LIMITED`, `LIMITÉE`, `LTD.`, `LTÉE`. Rationale: matches what corporate registries actually require on filings. The "named" path previously asked the user to type the ending into the free-text business-name field, which mixed two pieces of data into one input.
- **New shared module** [src/lib/legalEndings.ts](src/lib/legalEndings.ts): exports `LEGAL_ENDINGS` const tuple, `LegalEnding` type, and `legalEndingSchema` (Zod enum with custom "Select a legal ending" message). Single source of truth shared by wizard + API.
- **Wizard Step 3 (`src/app/incorporate/page.tsx`)**:
  - `WizardData.legalEnding: LegalEnding | ""` (empty allowed in init; Zod refines to a non-empty enum value at validate).
  - `s3` Zod schema now requires `legalEnding`.
  - For "named" corps: the proposed-name field's placeholder dropped the trailing `Inc.` (was `e.g. "Acme Technologies Inc."`, now `e.g. "Acme Technologies"`); hint changed to "Enter the distinctive part of the name only — the legal ending is selected separately below."
  - For "numbered" corps: explainer blurb updated to say "combined with your selected legal ending below (e.g. *1234567 Canada INC.*)".
  - **NEW Legal Ending `<select>`** rendered for both name types, with `-- Please Select --` placeholder and a contextual hint based on name type.
  - Step 7 (Review) Corporation row now shows the full filed name (named: `${businessName} ${ending}`) or the numbered template (`Numbered corporation (${jurisLabel}) — ${ending}`).
  - Wizard payload sent to `/api/incorporate` now includes `legalEnding`.
- **`/api/incorporate` (`src/app/api/incorporate/route.ts`)**:
  - Zod payload schema requires `legalEnding`.
  - Stripe Checkout line-item description now uses `${businessName} ${legalEnding}` (named) or `Numbered corporation (Federal) — ending: INC.` (numbered).
  - Stripe session metadata includes `legalEnding`, so the webhook can surface it on the [PAID] email.
  - Intake email subject and body now include the legal ending: subject reads e.g. `[PENDING] KPX-... — Acme Technologies INC. — Standard — $704.97 CAD`. New "Legal ending" row added to the Order section of the [PENDING] email body.
- **`/api/stripe-webhook` (`src/app/api/stripe-webhook/route.ts`)**: reads `legalEnding` from session metadata; surfaces it in the [PAID] subject (`Numbered Federal INC.` / `Acme Technologies INC.`), and adds a "Legal ending" row to the email body. Same handling for the `[PAYMENT FAILED]` async path.
- **Verified**: `npx tsc --noEmit` clean, `npm run lint` clean, `npm run build` green. Routes still 24. `/incorporate` bundle 60.7 kB → 61 kB (+0.3 kB for the new dropdown). Visual walkthrough on `localhost:3000/incorporate`:
  1. **Named, default** ([build-tmp/step3-verify/01-named-default.png](build-tmp/step3-verify/01-named-default.png)): Named tile selected, "Proposed Corporation Name" with new placeholder + hint, **Legal Ending dropdown showing "-- Please Select --"** with the explanatory hint, NUANS callout still present, all downstream fields (NAICS, business activity, fiscal year) intact.
  2. **Numbered** ([build-tmp/step3-verify/02-numbered.png](build-tmp/step3-verify/02-numbered.png)): Numbered tile selected, name field hidden, updated explainer blurb, Legal Ending dropdown still visible with its numbered-specific hint.
  3. **Dropdown content** verified by curl: all 9 endings present in the rendered HTML in the exact order requested (CORP. / CORPORATION / INC. / INCORPORATED / INCORPORÉE / LIMITED / LIMITÉE / LTD. / LTÉE).
- **Backwards-compat consideration**: existing in-flight Stripe sessions (none expected — live mode hasn't been used by real customers yet) wouldn't carry the `legalEnding` metadata key. The webhook handles the absent-key case by emitting an empty suffix and skipping the "Legal ending" row. Subject lines on those legacy sessions look the same as before. No defensive code paths needed in pricing or schema.

### 2026-04-24 (homepage copy tweaks)
- `src/app/page.tsx` hero subtitle: dropped trailing `alike` — now ends "available to Canadian entrepreneurs and international founders."
- `src/app/page.tsx` "Why Korporex" section heading: `Built for Canadian Entrepreneurs` → `Built for Entrepreneurs Like You`. Reframes from a geographic claim to a more inclusive customer-voice headline (still consistent with the subtitle directly above which references both Canadian + international founders).
- About page ([about/page.tsx:23](src/app/about/page.tsx) and `:51`) carries similar "Canadian and international founders alike" wording — mirrored both in a follow-up commit per user direction. `grep -r alike src/` now returns no matches.

### 2026-04-24 (registered office add-on — collapsed to single GTA tier, non-refundable)
- **Decision (from user)**: Eliminate the two-tier (Basic Mississauga / Premium Bay Street) registered-office add-on. Replace with a single option:
  - Either the customer provides their own registered office address, OR
  - They use Korporex's address — somewhere in the Greater Toronto Area, **chosen by Korporex at our discretion** and not advertised in advance.
  - Price: **$99.99/month, billed annually in advance** ($1,199.88 + HST).
  - **Non-refundable** — including if the customer obtains their own registered office address before the term ends.
  - Service includes a monthly scanned copy of mail received at the address, emailed to the customer.
- **`src/lib/pricing.ts`**: collapsed `REG_OFFICE_ADDON` from a `{basic, premium}` record into a single object. Type `RegOfficeAddon` is now `"none" | "korporex"`. The `address` sub-object holds sentinel values (`street: "Assigned by Korporex at filing"`, `postalCode: "TBD"`, `city: "Greater Toronto Area"`) that flow into the wizard's `regOffice` fields when the add-on is picked, so the [PENDING] email surfaces a clear "to be filled in before filing" marker for ops.
- **`src/app/incorporate/page.tsx` (wizard Step 6)**: picker reduced from 3 options → 2 (own / Korporex GTA). When "Korporex" is selected, address fields are hidden and replaced with an info card listing the three commitments (monthly mail scans, address appears on Articles + public registry, **non-refundable** including early-exit). Step 7 (Review) "Registered Office" line now shows "Korporex Registered Office — Greater Toronto Area" rather than a city. Zod schema for s6 narrowed to `["none", "korporex"]`.
- **`src/app/api/incorporate/route.ts`**: schema enum narrowed; Stripe line item renamed `Korporex Registered Office (12 months)` with description "Greater Toronto Area. Korporex assigns the address before filing. Monthly mail scans emailed to customer. $99.99/mo × 12. Non-refundable."; intake email Registered Office section now carries a yellow "ACTION REQUIRED — assign actual GTA address before filing. Customer paid 12-month non-refundable term." banner when the add-on is chosen.
- **`src/app/api/stripe-webhook/route.ts`**: [PAID] email row simplified to `${REG_OFFICE_ADDON.label} — Greater Toronto Area (12 months, non-refundable)` for the chosen-add-on case.
- **`src/app/pricing/page.tsx`**: replaced the 2-card add-on grid with a single-card section. Copy: "Registered office address in the Greater Toronto Area, chosen by Korporex" + monthly scan + appears on Articles. Footer note: "Non-refundable. The annual fee is billed in advance and is not refundable, in whole or in part, including if you obtain your own registered office address before the end of the term."
- **`known-issues.md`**: lowered the previous high-sev "placeholder addresses" issue to medium and re-scoped it as an operational reminder — sentinel address is now intentional design, ops must substitute the real GTA address before filing per the [PENDING] email's banner.
- **Verified**: `npx tsc --noEmit` clean, `npm run lint` clean, `npm run build` green. Routes still 24. Static prerender of `/pricing` confirms new copy: contains `Greater Toronto Area`, `Korporex Registered Office`, `Non-refundable`, `99.99`, `1199.88`, `chosen by Korporex`, `annually in advance`; no longer contains `Mississauga`, `Bay Street`, `Financial District`, `49.99`, or `599.88`.
- **Not yet done in this pass**: visual browser walkthrough of `/incorporate` Step 6 (build + types pass and the picker logic is straightforward, but a manual click-through at `localhost:3000/incorporate` is worth doing). User should also do the real-card Stripe verification run since this rewires the line items.
- **Next**: commit + push (will deploy to `korporex.vercel.app`; `korporex.com` continues to serve `/soon` via middleware until launch mode is lifted).

### 2026-04-24 (afternoon polish — /soon header cleanup + homepage hero rotator)
- **/soon landing page — header cleanup** (two commits):
  - `c0146d2` Removed the Next.js `<Image>` of `logo-mark.svg` from the header. Was rendering as a broken-image icon because serving SVG through `next/image` requires `images.dangerouslyAllowSVG` in `next.config.mjs`, which this project doesn't set. Simpler to remove the image than enable SVG optimization.
  - `236230c` Removed the "Korporex" wordmark too per user feedback that it didn't look good. Header is now a minimal strip with only the `contact@korporex.com` mailto link right-aligned. Branding carries through in the hero copy and footer copyright.
- **Homepage hero — animated rotator between "Business Incorporation." and "Corporate Services."** Sequence: `0e796bf` → `3a6773a` → `f801c55` → `2f2098b` → `9f00dae`. Final behaviour:
  - Words reveal one at a time with a rise + fade-in (Business → Incorporation. → hold → fade → Corporate → Services. → hold → fade → loop). Timings on the component are `wordStaggerMs=550`, `holdMs=1800`, `fadeMs=450` — all adjustable props on `<RotatingWords>` in `src/app/page.tsx`.
  - `<br />` between the rotator and the gold "Easy. Fast." / "Affordable." line so the gold line always starts fresh below the rotating phrase (fixes a wrap where "Easy." had crept onto the same line as "Services.").
  - Word spacing expressed as `mr-[0.25em]` on preceding words rather than `ml-[0.25em]` on trailing words — so when "Incorporation." or "Services." wrap to a second line, they start flush against the container's left edge instead of being indented by the inherited left margin.
  - New component `src/components/RotatingWords.tsx` (client). Accessible via `aria-live="polite"`, honours `prefers-reduced-motion` (shows first phrase without animating).
- **Visual verification** via headless Chrome captures from `https://korporex.vercel.app/` at `--virtual-time-budget` of 500 / 1400 / 4100 ms — all three states (first word alone, full first phrase, full second phrase) render correctly; gold "Easy. Fast." / "Affordable." sits on stable lines below throughout.
- **Tracking-file state**: repository working tree clean at commit `9f00dae` — all homepage + /soon + middleware + DNS work is committed and pushed. `progress.md` now fully reflects the full 2026-04-24 session.

### 2026-04-24 (DNS cutover — coming-soon page LIVE at korporex.com)
- **DNS re-cut via Cloudflare API** using a fresh short-lived token (1-day TTL, `Zone:DNS:Edit` scope on korporex.com only, token ID `cda2539d16307cdf0db05a9888347395`). **User should revoke the token now that the cutover is complete.** Three record changes executed on zone `5604175ce607a8a5b093d00251a794c6`:
  1. `PUT` on CNAME `www.korporex.com` (record `7a99e783d42a0f8bf29a92e4640867d5`) — content `korporex.com → a2768dd48b0e9d48.vercel-dns-017.com`, `proxied: true → false`.
  2. `DELETE` A `korporex.com → 162.213.255.25` (record `5908acca3a3ad94f9ec50e3af9711bcc`) — the Namecheap WordPress shared-hosting IP.
  3. `POST` new CNAME `korporex.com → a2768dd48b0e9d48.vercel-dns-017.com`, proxied off. Works at apex via Cloudflare's native CNAME flattening.
- **Email records untouched.** Verified post-cutover via Cloudflare API: 3× MX (route{1,2,3}.mx.cloudflare.net), SPF (`v=spf1 include:_spf.mx.cloudflare.net include:spf.brevo.com ~all`), DMARC (`v=DMARC1; p=none; rua=mailto:rua@dmarc.brevo.com`), Brevo DKIM CNAMEs (`brevo1/2._domainkey.korporex.com → b1/b2.korporex-com.dkim.brevo.com`), Brevo domain-verification TXT (`brevo-code:254c26f...`) — all present and correct. Inbound `contact@korporex.com` → Gmail, outbound via Gmail "Send mail as" + Brevo SMTP relay continue uninterrupted.
- **Vercel SSL cert provisioned.** Both apex and www got their Let's Encrypt certs within ~5 min of the DNS change. The local Windows DNS resolver took significantly longer to pick up the new records than public resolvers did (8.8.8.8 / 1.1.1.1 both updated within a minute; local Windows resolver cached the NXDOMAIN from the A record's deletion and kept serving stale negative results for several minutes) — verified using `curl --resolve korporex.com:443:64.29.17.65` to bypass local cache, which succeeded immediately.
- **Full verification battery (all curl'd with `--resolve` to force-bypass local Windows DNS cache)**:
  - `https://korporex.com/` → HTTP 200, 26686 bytes, cert valid (`ssl_verify_result=0`), contains "Launching Soon" + "Tell Us About Your Business" ✅
  - `https://korporex.com/pricing` → HTTP 200, **26686 bytes** (same as /soon, not the ~34 KB pricing page), contains "Launching Soon" only — middleware rewrite working, full pricing page correctly hidden ✅
  - `https://korporex.com/incorporate` → HTTP 200, 26686 bytes, serves /soon content (WIP wizard hidden from public) ✅
  - `https://www.korporex.com/` → HTTP 200, 26686 bytes, cert valid, serves /soon ✅
  - `https://korporex.vercel.app/pricing` → HTTP 200, 34601 bytes, **full pricing page with KORPOREX nav** — dev path unaffected by middleware ✅
- **Middleware details** (`src/middleware.ts`, commit `8eec8b6`). Host-header gate: if `Host` ∈ {`korporex.com`, `www.korporex.com`}, rewrite to `/soon` unless path is `/soon`, `/api/*`, `/_next/*`, or `/favicon.ico`. Matcher further excludes `_next/static` and `_next/image` to avoid invoking middleware on static assets. Middleware bundle 26.6 kB. Removing launch mode = delete the file, commit, push.
- **Vercel deploy `korporex-dmg6dzw4o`** (Production, Ready, 37s build) is serving the middleware + /soon page. The subsequent progress-log push (`848150a`) triggered deploy `korporex-dyk00wmn5` — also Production, Ready.
- **`NEXT_PUBLIC_SITE_URL` intentionally left as `https://korporex.vercel.app`** — the /soon page's contact form posts to `/api/contact` which doesn't depend on this env var, and the full incorporation flow continues to live at `.vercel.app` during launch-mode so Stripe success/cancel URLs remain correct during dev/testing. When launch mode is eventually removed, flip this back to `https://korporex.com` and redeploy.
- **Next (no urgency, user-driven)**: (1) revoke the Cloudflare API token, (2) continue the ship-blocker work — real reg-office addresses + legal-drafts review + real-card Stripe verification run, (3) when ready to flip to full site, delete `src/middleware.ts`, update `NEXT_PUBLIC_SITE_URL` to `https://korporex.com`, push.

### 2026-04-24 (launch-mode middleware deployed — awaiting DNS)
- **`src/middleware.ts` added.** When `Host` is `korporex.com` or `www.korporex.com`, rewrite every non-`/api` / non-`/_next` / non-`/favicon.ico` path to `/soon`. Matcher excludes `_next/static` and `_next/image` to keep static-asset requests from invoking middleware. `LAUNCH_MODE_HOSTS` is a `Set` so additional hostnames (e.g., `korporex.ca` if ever added) can be included with one line. Removing launch mode = delete `src/middleware.ts` and redeploy.
- **Verified in production** against deploy `korporex-dmg6dzw4o` (commit `8eec8b6`):
  - `curl -H "Host: korporex.com" https://korporex.vercel.app/pricing` → **HTTP 200, 26686 bytes, contains "Launching Soon" and "Tell Us About Your Business"**, does NOT contain "Pricing" / "NUANS" / "Federal — Standard" strings → proves `/pricing` is being rewritten to `/soon` content when Host header matches.
  - `curl https://korporex.vercel.app/pricing` (default host) → **HTTP 200, 34601 bytes, contains the full Pricing page** (KORPOREX navbar, Basic/Standard/Premium tiers) → proves `*.vercel.app` is untouched.
- **Committed & pushed** as `8eec8b6` ("Add /soon coming-soon landing page + launch-mode middleware for korporex.com"). Vercel auto-deployed successfully; deploy `korporex-dmg6dzw4o` is Ready and Production-tagged. Middleware bundle is 26.6 kB.
- **Blocker on go-live: DNS re-cutover.** `korporex.com` DNS currently still points at the old Namecheap WordPress IP (`162.213.255.25`) per the 2026-04-23 revert. To make the coming-soon page live at the real domain, need a fresh short-lived Cloudflare API token (`Zone:DNS:Edit` scope on `korporex.com`, 1-day TTL, revoke after use) to replay the original cutover: (1) CNAME `www.korporex.com` → `a2768dd48b0e9d48.vercel-dns-017.com`, proxy off; (2) delete `A korporex.com → 162.213.255.25`; (3) add CNAME `korporex.com → a2768dd48b0e9d48.vercel-dns-017.com`, proxy off. Propagation + SSL provisioning takes 1–5 min; monitor via `curl https://korporex.com/` with `--resolve` override.
- **`NEXT_PUBLIC_SITE_URL` on Vercel stays as `https://korporex.vercel.app`** for now — the /soon page's contact form posts to `/api/contact` which doesn't care about this env var, and the full incorporation flow continues to live at `.vercel.app` during launch-mode so Stripe success/cancel URLs remain correct. When the full site goes live (middleware removed), `NEXT_PUBLIC_SITE_URL` should be flipped back to `https://korporex.com` and redeployed.

### 2026-04-24 (homepage services showcase + /soon landing page)
- **Homepage — new "Complete Corporate Services" section** (`src/app/page.tsx`). Added a cream-background section between the Jurisdictions grid and "How It Works" that surfaces the four non-incorporation service categories (Registrations, Changes & Amendments, Compliance Filings, Business Updates) as four hover-interactive cards — each with a lucide icon, bold title, short description, three bullet examples, "From $X" price floor, and a "Learn more →" tag that links to `/services`. Cards flip from white to forest-green on hover (same `group` / `group-hover:` pattern used elsewhere). A "Browse All Services" outlined CTA below the grid links to the full `/services` page. Previously there was only a single underlined "View all services…" text link beneath the Jurisdictions section — which left the impression that Korporex did incorporation only. Icon set imported: `FileText`, `Edit3`, `ClipboardCheck`, `RefreshCw` (added to the existing `lucide-react` import). Home bundle grew from ~2.0 kB → 2.45 kB (+0.45 kB).
- **New `/soon` route — branded coming-soon landing page** (`src/app/soon/page.tsx`). Full-viewport page with:
  - Navy top strip (logo mark + "Korporex" wordmark left, `contact@korporex.com` mailto link right)
  - Hero (navy background, gold accents): "LAUNCHING SOON" badge with pulsing dot, large "Something great is on the way." serif headline with `great` in gold, two-paragraph messaging explaining the full site is coming and that orders are being taken now, gold-outlined "We respond within 24 hours" chip
  - Right column: the `SoonContactForm` (new component — `src/components/SoonContactForm.tsx`) with Name / Email / service-interest select (8 options covering all categories) / Message fields, validation, submitting state, and a styled thank-you panel on success
  - White capabilities strip below the fold: "WHAT WE DO" eyebrow with a 5-icon grid (Incorporation / Registrations / Amendments / Compliance / Business Updates), each with a short sub-line — same icon set as the services page
  - Minimal cream footer with copyright, contact email, and the not-a-law-firm disclaimer
  - Form posts to the existing `/api/contact` route with `source: "soon"` so operators can filter these separately from hero / contact-page submissions in Gmail
- **Chrome bypass on `/soon`** (`src/components/layout/SiteChrome.tsx` + `src/app/layout.tsx`). Added a tiny client wrapper `SiteChrome` that reads `usePathname()` and hides the main site Navbar + Footer (and drops the `pt-[72px]` for the fixed navbar) when the path is `/soon` or any descendant. Root layout now renders `<SiteChrome>{children}</SiteChrome>` instead of the inline `Navbar` + `<main>` + `Footer` trio. This keeps the rest of the site's chrome untouched and avoids a `(site)` / `(launch)` route-group refactor. Extensible via a single `STANDALONE_PREFIXES` array in `SiteChrome.tsx` if more chrome-less pages are added later.
- **Metadata**: `/soon` has its own `<title>` ("Korporex — Launching Soon") and description, independent of the root layout's site metadata, so social shares and search-engine snippets are correct when that page is the public face.
- **Verified**: `npx tsc --noEmit` clean, `npm run lint` clean, `npm run build` green. Routes: 23 → **24** (`/soon` is statically prerendered as `○ Static` at 7.95 kB / 95.2 kB first-load). Home bundle 2.0 kB → 2.45 kB (+services section). No other page weights changed.
- **Not done in this pass** (visual browser verification): dev server wasn't started — build passing and the fact that `/soon` was pre-rendered statically during build is strong evidence the page renders without runtime errors, but a manual walkthrough at `http://localhost:3000/soon` is worth doing before deploy. Run `npm run dev` and navigate to `/soon`.
- **Deployment options when ready** to replace the WordPress site at `korporex.com` with this page:
  1. Simplest: after the DNS re-cutover, add a Vercel rewrite in `next.config.mjs` mapping `/` → `/soon` (or set up a middleware rewrite gated by an env flag `LAUNCH_MODE=coming-soon`), so the root of `korporex.com` serves the landing page while the full site stays accessible under paths like `/soon/incorporate` (or is temporarily gated).
  2. Alternative: keep the full site at `korporex.vercel.app`, deploy a SEPARATE minimal Vercel project pointed at `korporex.com` containing only the `/soon` content at the root — totally isolated; the main project stays untouched. Lower risk of accidental exposure of in-progress work.
  3. Third option: on cutover, route `korporex.com` to this project but add a Vercel domain-specific rewrite so the apex + `www` serve `/soon`, while `korporex.vercel.app` continues to serve the full site. Same idea as (1) but at the edge, without code changes.
- **Next**: user to (a) visually QA the `/soon` page at `localhost:3000/soon` and the new homepage services section at `localhost:3000/`, (b) pick a deployment approach from the three options above when ready to re-cut DNS, (c) confirm the services-section copy/pricing floors ("From $79 / $99 / $99 / $199") are accurate — they were derived from the `/services` page data.

### 2026-04-23 (DNS revert — back to WordPress while dev continues)
- **User asked to revert DNS** so `korporex.com` serves the old WordPress site again while they continue finishing the Next.js build, to avoid exposing in-progress work to the public. Created a fresh Cloudflare API token (1-day TTL, same `Zone:DNS:Edit` scope on `korporex.com`), executed the reverse of this morning's cutover, and the user revoked the token immediately after.
- **Three Cloudflare record changes (reverse of the cutover)**:
  1. `CNAME www.korporex.com` — content `a2768dd48b0e9d48.vercel-dns-017.com` → `korporex.com`, proxy `false → true`.
  2. Deleted the apex `CNAME korporex.com → a2768dd48b0e9d48.vercel-dns-017.com`.
  3. Added `A korporex.com → 162.213.255.25` (Namecheap WordPress shared-hosting IP), proxy `true` (orange cloud). Matches the exact state of the record before the morning's cutover.
- **Vercel env + redeploy**:
  - `NEXT_PUBLIC_SITE_URL` in Production scope reverted to `https://korporex.vercel.app`
  - Triggered production redeploy `korporex-a7hjfeqzn` (Ready, 38s build). Now Stripe Checkout success/cancel URLs match the URL customers use during testing (`.vercel.app`), so no cross-domain bounce after payment.
- **Verified**:
  - `https://korporex.com/` → HTTP 200, 428 KB, contains the "WordPress" string (original WordPress home page back in place).
  - `https://www.korporex.com/` → HTTP 301 redirect (normal WordPress www→apex behaviour).
  - `https://korporex.vercel.app/terms` → HTTP 200, 67860 bytes (Next.js app fully functional for dev).
  - Vercel Domains panel will show `korporex.com` and `www.korporex.com` as ⚠️ Invalid Configuration again — harmless, just the flag that DNS isn't pointed here. Kept the domains registered in Vercel so a future re-cutover is a 30-second operation instead of re-adding them.
- **What still works identically**: live-mode Stripe (keys, webhook at `https://korporex.vercel.app/api/stripe-webhook`, all four env vars), Brevo transactional email, Cloudflare inbound email routing (`contact@korporex.com` → Gmail), Gmail outbound "Send mail as" via Brevo SMTP. The revert touches DNS A/CNAME records only; no email or payment infrastructure disturbed.
- **To re-launch on `korporex.com` later**: the user creates a fresh Cloudflare token → I re-apply the 2 CNAMEs pointing at `a2768dd48b0e9d48.vercel-dns-017.com` (proxy off), delete the A record, flip `NEXT_PUBLIC_SITE_URL` to `https://korporex.com`, redeploy. Propagation + SSL takes 1–5 minutes. Same process as this morning's cutover but cleaner second time around because nothing else has to be decided.

### 2026-04-23 (post-cutover — SITE_URL env + verification)
- **Cutover confirmed live at 18:22:45** by the polling monitor: `https://korporex.com/terms` → HTTP 200 with 67,860-byte Next.js payload (WordPress was ~300 KB, unmistakable diff).
- **Updated `NEXT_PUBLIC_SITE_URL`** in Vercel Production scope from `https://korporex.vercel.app` → `https://korporex.com` (removed + re-added via Vercel CLI since env-var mutations don't accept in-place value edits in the CLI).
- **Triggered production redeploy** — new deploy `korporex-gjyf4op6d`, Ready in 36s. `NEXT_PUBLIC_*` vars are build-time so this redeploy bakes the new origin into the wizard's Stripe call. Customers hitting checkout on korporex.com now get `success_url` = `https://korporex.com/incorporate/confirmation?...` instead of the `.vercel.app` subdomain.
- **End-to-end verification** (all HTTP 200, Next.js-sized payloads, valid SSL): `korporex.com/`, `/terms`, `/privacy`, `/incorporate`, and `www.korporex.com/terms`. Zero WordPress strings anywhere.

### 2026-04-23 (DNS cutover — Namecheap WordPress → Vercel)
- **Executed DNS cutover via Cloudflare API** using a short-lived, scoped API token (1-day TTL, `Zone:DNS:Edit` permission for korporex.com only, revoked immediately after use). Three record changes:
  1. `CNAME www.korporex.com` — content `korporex.com` → `a2768dd48b0e9d48.vercel-dns-017.com`, proxy `true → false`.
  2. Deleted `A korporex.com → 162.213.255.25` (the Namecheap shared-hosting IP, previously proxied through Cloudflare).
  3. Added `CNAME korporex.com → a2768dd48b0e9d48.vercel-dns-017.com`, proxy disabled. Works at apex via Cloudflare's native CNAME flattening.
- **All email-related and ancillary records left untouched** — MX (`route{1,2,3}.mx.cloudflare.net`), SPF (`v=spf1 include:_spf.mx.cloudflare.net include:spf.brevo.com ~all`), Brevo DKIM (`brevo{1,2}._domainkey`), Cloudflare Email Routing DKIM (`cf2024-1._domainkey`), cPanel leftover `default._domainkey`, DMARC, Brevo domain-verification TXT, all ten cPanel/WHM A records (`autoconfig`, `autodiscover`, `cpanel`, `cpcalendars`, `cpcontacts`, `ftp`, `mail`, `webdisk`, `webmail`, `whm` — all pointing at Namecheap `162.213.255.25`), and all SRV/TXT "path=/" cPanel discovery records. Email inflow to `contact@korporex.com` → Gmail continues uninterrupted; outbound via Brevo SMTP relay continues uninterrupted.
- **Google DNS resolver (8.8.8.8) already serving new records** (`64.29.17.65`, `216.198.79.65` — Vercel's IPs). Cloudflare's public resolver (1.1.1.1) still held stale cached Cloudflare edge IPs at the time of commit — normal cache propagation lag, clears within 5 min.
- **Currently waiting on**: Vercel SSL cert auto-provisioning for korporex.com and www.korporex.com. `curl --resolve korporex.com:443:64.29.17.65 https://korporex.com/terms` returned SSL handshake failure at cutover moment — expected within first minutes. A Monitor task is polling `korporex.com/terms` every 20s and will fire an event when HTTP 200 with Next.js-sized payload (30–100 KB; WordPress page is ~300 KB, Next.js `/terms` is ~70 KB) is served. Once that fires:
  - Update `NEXT_PUBLIC_SITE_URL` env var in Vercel Production → `https://korporex.com`
  - Trigger production redeploy (so Stripe success/cancel URLs use the new domain)
- **Deliberately NOT migrated yet**: Stripe webhook endpoint URL stays at `https://korporex.vercel.app/api/stripe-webhook`. The `.vercel.app` alias is permanent, customer-invisible, and keeps working — zero urgency to migrate. Nice-to-have cleanup for a later session.
- **Confirmed finding** (needed clearing up earlier in session): DNS for korporex.com is authoritatively served by Cloudflare (nameservers `doug.ns.cloudflare.com`, `melany.ns.cloudflare.com`), delegated at Namecheap where the domain is registered and WordPress hosting lives. Vercel Domains panel detected this correctly and showed "DNS Records to change on Cloudflare" rather than the Namecheap-hosted-DNS flow.

### 2026-04-23 (Stripe production switch)
- **Stripe production is live on Vercel.** Discovered via `vercel env ls` that no `STRIPE_*` env vars existed in Vercel before this session — the production deploy was silently dropping into the dev-fallback path in `src/lib/stripe.ts` (no actual payment collection). Walked the user through activating and configuring live mode step by step.
- **Webhook endpoint created in Stripe Dashboard (live mode)**: name `Korporex production — checkout`, URL `https://korporex.vercel.app/api/stripe-webhook` (using Vercel's stable `*.vercel.app` alias because DNS is not yet cut over), subscribing to `checkout.session.completed`, `checkout.session.async_payment_succeeded`, `checkout.session.async_payment_failed`. API version `2025-09-30.clover`. Signing secret generated and stored in Vercel.
- **Added four env vars to Vercel (Production scope only, Preview/Development unchecked)**:
  - `STRIPE_SECRET_KEY` — `sk_live_...` (sensitive)
  - `NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY` — `pk_live_...` (not sensitive, client-exposed by design)
  - `STRIPE_WEBHOOK_SECRET` — `whsec_...` (sensitive, this is the live-mode secret from the new endpoint, NOT the `stripe listen` local secret)
  - `NEXT_PUBLIC_SITE_URL` — `https://korporex.vercel.app`
- **Triggered production redeploy** (`vercel deploy --prod --yes`) so the `NEXT_PUBLIC_*` vars bake into the bundle. New deploy `korporex-jrtiyr0zg` finished Ready in 40s. Both the latest deploy and the previous `korporex-p1y5y2snj` are now in Production; aliases (`korporex.vercel.app`, `korporex-youness-7473s-projects.vercel.app`, `www.korporex.com`, `korporex.com`) point at the newest Ready deploy.
- **DNS finding surfaced during Stripe setup**: `korporex.com` is in Vercel's domain list (and aliased to the latest deploy) but DNS resolution is still pointing at WordPress for the apex — a curl of `https://korporex.com/terms` returns 404 HTML containing "WordPress" while `https://korporex.vercel.app/terms` returns the Next.js page at 200. Webhook URL therefore uses the `.vercel.app` alias; when DNS cutover happens, Stripe webhook endpoint URL must be updated (either add a second destination at the new URL or edit the existing one).
- **Still pending before real customers can be directed to production**:
  1. **Verification run**: complete one real-card incorporation on `https://korporex.vercel.app/incorporate` (smallest order, e.g., Basic Ontario numbered = $399) to confirm live keys + webhook + [PENDING]/[PAID] emails all work together. Stripe Dashboard → Payments will show the charge; Dashboard → Webhooks → the destination will show a 200 OK delivery.
  2. Replace placeholder registered-office addresses in `src/lib/pricing.ts` before the add-on can be sold.
  3. DNS cutover and corresponding Stripe-webhook URL update.
- **Scope note**: live-mode Preview scope is intentionally empty for Stripe. If preview deploys become active, we'd want to create a separate Stripe account or test-mode-only webhook endpoint for preview scope rather than sharing live keys with branch deploys.

### 2026-04-23 (after initial push — legal-entity + no-refund updates)
- **Legal entity name locked in**: `Korporex Business Solutions Inc.` — a Canadian corporation incorporated under the Canada Business Corporations Act with its head office in Ontario. Updated:
  - `src/app/terms/page.tsx` Section 1 "About Korporex" now identifies the operating entity explicitly and scopes the defined terms ("Korporex", "we", "us", "our") to Korporex Business Solutions Inc. and its affiliates.
  - `src/app/privacy/page.tsx` opening paragraph rewritten to lead with the legal entity and CBCA context before the Site description.
  - `src/components/layout/Footer.tsx` copyright line corrected from "© 2025 Korporex Technologies Inc." (wrong entity name, wrong year) to "© 2026 Korporex Business Solutions Inc." Same not-a-law-firm disclaimer retained.
- **Refund policy locked in**: **no refunds once payment submitted.** Simpler than the graduated policy I drafted earlier; reflects operational reality (work is commenced immediately on payment — intake email fires, Stripe session created, any downstream third-party fees incurred). Updated:
  - Terms §6 renamed from "Refunds and Cancellations" to "No Refunds"; rewritten with three paragraphs — (1) blanket no-refund once paid applying to package / NUANS / registered-office / taxes, (2) narrow Korporex-error rectification exception (re-file at no extra service cost; still can't refund government fees the registry keeps), (3) exceptional-circumstance escalation to contact@ with explicit "no obligation to issue a refund" language.
  - FAQ "What is your refund policy?" rewritten to mirror the Terms policy with a pointer to the full clause.
- **NOTE TO REVIEWERS comments** at the top of both legal pages updated to note these are **living drafts** — entity name + refund policy are now resolved, but the documents should be revised as other operational processes solidify (real registered-office addresses, confirmed filing timelines, any future analytics/user-accounts/data-export features).
- **Memory saved**: `project_korporex_legal_entity.md` records the entity name, no-refund policy, and the living-documents convention so future sessions pick up these as established project facts.
- **Tracking**: known-issues entry for "legal drafts unreviewed" **downgraded from high to medium severity** — the two most substantive unknowns (entity name, refund policy) are now resolved; remaining open items are cap-floor amount, governing-law confirmation, and the overall legal-review pass.
- **Verified**: `npx tsc --noEmit` clean, `npm run lint` clean. Vercel Production deploy from the earlier commit (`c49ae50`) finished Ready in 39s at `korporex-nufdol5tj` — Stripe + add-on + drafts are live on that preview URL now, and this second batch of copy updates will deploy automatically on the next push.

### 2026-04-23 (late — Terms of Service + Privacy Policy drafts)
- **Drafted Terms of Service** (`src/app/terms/page.tsx`) — 20-section document covering: acceptance, service description, explicit not-a-law-firm disclaimer (emphasized), eligibility, user responsibilities and representations, fees/taxes/pass-throughs (NUANS + government fees), refunds (pre-filing / post-filing / registered-office / Korporex errors), filing timeline and government delays, registered office service terms (mail handling, non-forwarding of parcels, termination for abuse), Korporex IP, user content licence, third-party services (Stripe/Brevo/Google/Cloudflare/Vercel/registries), acceptable use, warranties disclaimer, limitation of liability (cap at greater of fees paid or CAD $100), indemnification, suspension and termination, amendments, Ontario governing law, general provisions, contact. Effective date `April 23, 2026`.
- **Drafted Privacy Policy** (`src/app/privacy/page.tsx`) — 12-section PIPEDA-aligned document covering: information collected (incorporation wizard data, payment/billing data, registered-office mail, communications, technical/usage data — noting no analytics currently deployed), how data is used, legal basis and consent, sharing (government registries, Stripe, Brevo, Google Maps Platform, Cloudflare, Vercel, professional advisors, legal process, corporate transactions), international transfers (US/EU), retention periods per category (incorporation 7y, payment 7y, scanned mail service+12mo, email 7y, technical 30-90d), user rights (access/correction/consent-withdrawal/OPC complaint), cookies (strictly necessary only, no tracking), security (HTTPS/PCI-DSS via Stripe), children (18+), changes, and Privacy Officer contact. Effective date `April 23, 2026`.
- **Research approach**: attempted to fetch Ownr.co's Terms (/legal/terms) and Privacy (/document/privacy-policy) for reference as user requested. Ownr's pages are client-rendered + the Wayback Machine is blocked, so no exact text was retrievable — which conveniently precludes any accidental plagiarism. Drafts are instead based on Canadian SaaS industry norms, PIPEDA obligations, and Korporex's actual implemented data flows as captured in the codebase.
- **Wired placeholder links to real pages**: Footer (`src/components/layout/Footer.tsx`) "Privacy Policy" / "Terms of Service" links now point to `/privacy` / `/terms` instead of `#`. Wizard Step 7 disclaimer links (`src/app/incorporate/page.tsx`) likewise updated. Remaining `#` links in the Footer (LinkedIn, Twitter) are legitimate "coming soon" placeholders for social — left alone.
- **Email convention reconfirmed**: user reinforced that all email references in this project must be `contact@korporex.com`. Greps across the repo confirm 21+ references all resolve to `contact@korporex.com` — no stray `support@` / `noreply@` / `info@` / `hello@` / `admin@` addresses. Both new legal drafts follow the convention (7 references in terms, 4 in privacy, all correct).
- **Verified**: `npx tsc --noEmit` clean, `npm run lint` clean, `npm run build` green. Total routes 21 → **23** (`/privacy` and `/terms` both statically prerendered at 178 B + 96.1 kB first-load — tiny).
- **Known issue raised**: both legal documents are marked as **unreviewed drafts** in known-issues.md as a high-severity go-live blocker. User (a commercial lawyer) will do the review pass; specific items flagged for attention: refund policy terms, limitation-of-liability cap floor, governing-law choice, registered-office service clauses, and substitution of the actual legal entity name (currently placeholder "Korporex"). "NOTE TO REVIEWERS" comment blocks left at the top of each page file.

### 2026-04-23 (evening — add-on build + email polish)
- **Built the registered-office add-on end-to-end.** User-specified pricing (annual prepay, monthly display): Basic $49.99/mo = $599.88/yr (Ontario city of our choice) and Premium $99.99/mo = $1,199.88/yr (Toronto financial district). Both include monthly mail-scanning emailed to the customer. Plus HST (rolled into existing `getTaxRate` — same tax logic as the incorporation fee).
- **Pricing module** (`src/lib/pricing.ts`): new `RegOfficeAddon` type (`"none" | "basic" | "premium"`), `REG_OFFICE_ADDON` const with prices + labels + placeholder addresses, `regOfficeAddonAvailable()` helper gating the add-on to federal/Ontario only (BC requires a BC reg office by statute), and `computePricing()` extended with optional `regOfficeAddon` param plus a new `regOfficeFee` field in the breakdown.
- **Wizard Step 6 rewrite** (`src/app/incorporate/page.tsx`): Radio-button group with three options — "I'll provide my own" (shows address fields), "Korporex Basic" ($49.99/mo), "Korporex Premium" ($99.99/mo). Selecting a Korporex option hides the address fields and renders a read-only preview of our address with service notes. Selecting "own" restores the address inputs with existing region-lock behavior. BC jurisdiction continues to show only the own-address flow (no radio); copy points BC users to email us.
- **Step 7 review**: order summary now shows "Registered Office: Korporex Basic — Mississauga, ON" (or customer's own address); pricing breakdown has a new line for the add-on fee between NUANS and subtotal. Tax recalculates to include the add-on automatically.
- **Intake API** (`src/app/api/incorporate/route.ts`): Zod schema accepts `regOfficeAddon`, passes it through to `computePricing` for server-side recalc, adds a Stripe line_item with proper naming/description when the add-on is selected (`"Registered office — Basic (12 months)"` / `"$49.99/mo × 12"`), and includes the tier in Checkout Session metadata so the webhook can surface it. Intake HTML email now shows the add-on line in the pricing table plus a gold-accent "Korporex Basic — Ontario" header above the registered-office address.
- **Stripe webhook** (`src/app/api/stripe-webhook/route.ts`): pulls `regOfficeAddon` from session metadata; [PAID] email body now includes "Registered office add-on: Korporex Premium — Toronto — Financial District (12 months)" row when applicable. [PAID] subject was already normalized earlier in the session.
- **Marketing**: `/pricing` has a new "Registered Office Address Service" section with two cards (Basic / Premium) below the jurisdiction tiers, with the BC caveat. FAQ "Do I need a physical office address" answer rewritten with concrete pricing for both tiers.
- **Email subject normalization (earlier in session)**: `[PENDING]`, `[PAID]`, `[PAYMENT FAILED]` now share one format — `[STATUS] KPX-... — {displayName} — {pkgLabel} — ${total} CAD` — so matched pairs read cleanly when sorted in Gmail. Webhook now imports `JURISDICTION_LABELS`/`PKG_LABELS` for title-casing and reconstructs `displayName` from `corpNameType` + `businessName` the same way the intake does.
- **End-to-end test run**: completed `KPX-20260423-4BA8` (Ontario Numbered Standard, $676.87 CAD) successfully. Stripe webhook returned 200 on 5 events (`checkout.session.completed` + 4 others), both PENDING (2:50PM) and PAID (2:51PM) emails delivered to `contact@korporex.com` with matching ref, confirmation page rendered with `payment_status === "paid"` verified.
- **Verified**: `npx tsc --noEmit` clean, `npm run lint` clean, `npm run build` green (21 routes; wizard 60.1 kB / 157 kB first-load — +0.4 kB for add-on radios; pricing page 3.87 kB).
- **Known issue raised**: `REG_OFFICE_ADDON.basic.address` and `.premium.address` are **placeholders** (Mississauga / 181 Bay Street). User must replace with real addresses before letting paying customers select the add-on — logged as high-severity go-live blocker.
- **Next**: Commit and push everything (all pending Stripe + add-on + email + `.vercelignore` work is local-only). Then user needs to (a) confirm or correct the Premium tier monthly cadence (I assumed /month like Basic), (b) provide real physical addresses for Basic/Premium, (c) optionally set `NEXT_PUBLIC_GOOGLE_MAPS_API_KEY` in Vercel Preview scope via dashboard.

### 2026-04-23 (later)
- **Fixed Step 6 province-lock bug** (`src/app/incorporate/page.tsx:805`): `defaultValues.regOffice.region` used `??` to fall back to `regionLock`, but initial `data.regOffice.region` is `""` (empty string, not null/undefined), so `??` kept the empty string and Zod rejected submit as "Required" even though the disabled input visibly showed "ON"/"BC". Changed to `||` so empty strings also fall through to `regionLock`. Manual trigger: provincial (Ontario/BC) incorporation → Step 6 → click "Continue to Review" without editing any field → used to show "Required" under the locked Province. Now submits cleanly.
- **Removed false "registered office included" claims** across six files / eight lines:
  - Wizard package checklist (`incorporate/page.tsx` PKG_FEATURES): dropped "Registered office (1 month)" from Standard and "1-year registered office" from Premium.
  - Wizard package-select desc line (line 143): Premium desc no longer mentions registered office.
  - Wizard Step 6 copy: the "Standard & Premium packages include a registered office" paragraph replaced with a neutral "Don't have a physical address? Email contact@korporex.com about our registered office service." — truthful until the add-on is built.
  - `pricing/page.tsx`: removed "Registered office (1 month)" / "1-year registered office address" lines from Standard + Premium tiers across all three jurisdictions (federal, Ontario, BC).
  - `faq/page.tsx`: "Do I need a physical office address" answer rewritten to drop the "packages include for the first month / full first year" claim; now points international founders to email us about the add-on service.
- **Reason**: per user — none of the current packages actually bundle a registered office address, and registering that will be sold as a paid add-on as customers progress through the wizard.
- **Intentionally NOT built yet**: the add-on itself. Waiting on user spec for price + term (e.g., $199/yr) and whether to surface it as a toggle on Step 6 (recommended) or as a separate step. Once specified: add a `regOfficeAddon` boolean (+ optional term) to wizard state, conditionally pre-fill regOffice with Korporex's address, add an "Add-ons" line item to `computePricing` in `src/lib/pricing.ts`, surface it on Step 7 review + in the Stripe Checkout Session line items + in the PENDING/PAID email bodies.
- **Verified**: `npx tsc --noEmit` clean; no remaining matches for `1-year registered|Registered office \(1 month\)|packages include a registered`.
- **Wired `NEXT_PUBLIC_GOOGLE_MAPS_API_KEY` into local `.env.local`** and restarted dev. Autocomplete should now fire on `localhost:3000/incorporate`.
- **Surprise finding from Vercel env pull**: `NEXT_PUBLIC_GOOGLE_MAPS_API_KEY` was empty (`""`) in BOTH Preview and Production scopes on Vercel — progress's 2026-04-21 entry was stale, the value was unset at some point after that. Production autocomplete was also broken.
- **Fixed Vercel Production**: linked the project (`vercel link --project korporex`), removed the empty entry (`vercel env rm NEXT_PUBLIC_GOOGLE_MAPS_API_KEY production`), added the correct key (`vercel env add NEXT_PUBLIC_GOOGLE_MAPS_API_KEY production --value ...`), and triggered a redeploy (`vercel deploy --prod --yes`) so the value is baked into the production bundle (`NEXT_PUBLIC_*` are build-time). New production deploy `korporex-d7zqsdg7q` is Ready.
- **Created `.vercelignore`** excluding `build-tmp/`, `.env*.local`, `.claude/` — the first `vercel deploy` failed with an `EPERM` on a locked headless-Chrome session-storage file in `build-tmp/` that shouldn't have been uploaded anyway. Now future deploys skip that scratch folder.
- **Preview scope still unset**: Vercel CLI v51.8.0 refuses `vercel env add ... preview --value X --yes` with a `git_branch_required` error despite docs saying blank = "all preview branches". Tried stdin, `--no-sensitive`, explicit `--yes`, retrying — same error each time. Logged as a low-sev known issue with a dashboard fallback URL for the user to handle in 30 seconds if/when preview deploys become active.
- **Also surfaced from Vercel env pull** (not blocking, just noting for future context): a full Supabase + Postgres credential set exists on this Vercel project (added ~3 days ago — `POSTGRES_URL`, `SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY`, etc.), yet nothing in the Korporex codebase references Supabase. Either scaffolding from Vercel's Supabase marketplace integration started separately, or in-progress work tracked elsewhere. Left alone for now.

### 2026-04-23
- **Stripe Checkout (hosted) wired end-to-end.** Installed `stripe` v22.0.2 (API version `2026-03-25.dahlia`). New files:
  - `src/lib/stripe.ts` — server SDK singleton; returns `null` when `STRIPE_SECRET_KEY` is unset so dev is unblocked. Exports `getSiteUrl()` which prefers `NEXT_PUBLIC_SITE_URL` → `VERCEL_URL` → `localhost:3000` for building Stripe success/cancel URLs.
  - `src/lib/orderRef.ts` — generates `KPX-YYYYMMDD-XXXX` identifiers (31-char base32-ish alphabet, no ambiguous chars). Shared reference between the [PENDING] intake email and the [PAID] webhook email so operators can cross-reference.
  - `src/app/api/stripe-webhook/route.ts` — signature-verified webhook handler. Subscribes to `checkout.session.completed` (+ async variants), sends a "[PAID]" summary email to `contact@korporex.com` with order ref, amount paid, customer email, Stripe session/PI IDs. Uses raw body (`req.text()`) for HMAC verification.
- **`/api/incorporate` refactored**: now generates an order ref, sends the "[PENDING]" intake email (non-blocking — logged but doesn't break checkout if Brevo fails), then creates a Stripe Checkout Session with explicit line items (package, NUANS, tax — all derived from `@/lib/pricing` server-side), `customer_email` = primary director, `metadata.orderRef` (+ jurisdiction/pkg/billing for webhook use), and returns `{ url, orderRef }` to the client.
- **Step 7 redesigned**: removed all card fields (`cardNumber` / `expiry` / `cvc` / `cardholderName`) from the `s7` Zod schema, form defaults, and UI. The "Payment" subsection was replaced with a small note: "You'll be redirected to Stripe to complete payment securely." CTA is now "Continue to Payment — $X" (or "Redirecting to Stripe…" while submitting). On API success, `window.location.href = url` redirects to stripe.com; errors surface inline as before.
- **Confirmation page promoted to dynamic** (`force-dynamic`). Accepts `?session_id=...&ref=...`, retrieves the Stripe session server-side to verify `payment_status === "paid"`, and surfaces the order ref + amount paid in a small card under the hero. Falls back gracefully when neither is present (e.g., direct navigation) or when Stripe isn't configured (dev path via `?dev=1`). Copy updated: "You'll receive a Stripe receipt immediately" (Stripe sends receipts automatically if enabled in Dashboard → Customer emails).
- **`.env.local.example` + CLAUDE.md** updated with full Stripe setup instructions — test keys first (`sk_test_...` / `pk_test_...`), local webhook via `stripe listen --forward-to localhost:3000/api/stripe-webhook`, production endpoint at `https://<domain>/api/stripe-webhook` subscribing to `checkout.session.completed` + async variants.
- **Removed**: stale "No backend integration" / "Contact form submissions are stubbed" notes from CLAUDE.md — both API routes are now live.
- **Verified**: `npx tsc --noEmit`, `npm run lint`, `npm run build` all green. Build: 21 routes (`/api/stripe-webhook` and dynamic `/incorporate/confirmation` are new dynamic routes; `/api/incorporate` already existed). Wizard route shrank 60 kB → 59.7 kB with card fields removed.
- **User action required before first live test**:
  1. Copy `sk_test_...` and `pk_test_...` from Stripe Dashboard (Test mode toggle on) into `.env.local` (`STRIPE_SECRET_KEY` / `NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY`) and in Vercel (Preview/Development scope, not Production).
  2. Run `stripe listen --forward-to localhost:3000/api/stripe-webhook` in a second terminal → paste the printed `whsec_...` into `STRIPE_WEBHOOK_SECRET` in `.env.local`, restart `npm run dev`.
  3. Complete a test wizard end-to-end with card `4242 4242 4242 4242` to verify: redirect → payment → [PENDING] + [PAID] emails to `contact@` → confirmation page shows ref + amount.
  4. For production: Stripe Dashboard → Webhooks → Add endpoint → `https://<domain>/api/stripe-webhook`, subscribe to `checkout.session.completed`, `checkout.session.async_payment_succeeded`, `checkout.session.async_payment_failed`. Copy endpoint's signing secret to Vercel (Production scope). Swap keys to `sk_live_.../pk_live_...` in Vercel when ready to accept real payments.
- **Resolves**: high-sev "Incorporation wizard does not process payments" issue.

### 2026-04-23 (earlier)
- **Wizard intake backend live — `/api/incorporate` route handler.** Step 7's submit is now async: shows a "Submitting…" state, POSTs the full wizard payload (jurisdiction, package, corp name + NAICS + activity + fiscal year, directors[], shareholders[], registered office, billing name + address) to `/api/incorporate`, and navigates to the confirmation page only on success. Inline red error surface on failure with a fall-back contact address. **Card details are stripped client-side and never reach the server** — the Zod schema on the route explicitly excludes `cardNumber` / `expiry` / `cvc` / `cardholderName` so they can't be accidentally captured or emailed. Server recalculates price / NUANS / tax / total from the shared `@/lib/pricing` module using the submitted billing address, so the emailed summary reflects the authoritative numbers (not client-sent totals). Email body is a branded HTML summary with sections for Order, Pricing, Directors, Shareholders, Registered office, Billing, plus a notice that payment is pending Stripe. Dev fallback (no `BREVO_API_KEY`) logs the submission + computed pricing to the server console and returns `{ ok: true, dev: true }`.
- **Shared pricing module** — extracted `PRICES`, `NUANS_FEE`, `CA_TAX_RATES`, `getTaxRate`, `JURISDICTION_LABELS`, `PKG_LABELS`, `nuansApplies`, `computePricing` from the wizard into `src/lib/pricing.ts` so both UI and API compute from the same source. Wizard Step 7 now calls `computePricing(...)` instead of re-deriving inline. Type-check, lint, and production build all green (21 routes — `/api/incorporate` added; wizard route 60 kB / 157 kB first-load, +0.6 kB vs. previous).
- **Resolves**: high-sev "Incorporation wizard intake is not emailed / stored" issue. Remaining wizard gap narrowed to Stripe-only.

### 2026-04-22
- **Wizard polish — red required-asterisks + hover-green Named/Numbered cards.** Modified the shared `Field` component in `src/app/incorporate/page.tsx` to detect a trailing ` *` on labels and render the asterisk in `text-red-500`, so every required field across all 8 wizard steps now shows a red asterisk (one edit, ~25 form fields covered). Applied the `group` / `group-hover:` hover-to-forest-green pattern to the Named / Numbered selection cards on the Business Details step, with white title and gray-300 description on hover. Selected-state navy-50 tint preserved when not hovering. Type-check green.
- **Home jurisdiction cards — parity with pricing hover.** Applied the same `group` / `group-hover:` pattern to the Federal / Ontario / BC cards on the home page: card fades to forest green on hover, all text flips to white / gray-300/400. "Start" button changed from filled-navy to the outlined `border-navy-900 text-navy-900` default, flipping to gold-filled (`bg-gold-500 text-white`) on group-hover — matching the "Get Started" CTA on `/pricing`. Home + pricing now feel visually consistent.
- **Pricing page — hover state.** Added a `group` / `group-hover:` interaction so the hovered tier card flips to forest green (`bg-navy-900`) with white price, gray-200/300 supporting text, and a gold-filled CTA — all with `transition-colors` for a smooth swap. Cursor leaves → card returns to white. Replaces the static "Most Popular" highlight with a uniform-but-interactive treatment so all three tiers compete equally.
- **Pricing page — removed "Most Popular" treatment on Standard tier.** All three tiers (Basic / Standard / Premium) now render with identical styling — white card, gray border, navy text, gold check-marks, outlined CTA. Dropped the `featured` flag from the pricing data model and the conditional styling in `src/app/pricing/page.tsx`. Rationale: customer choice should be driven by the feature list, not a visual thumb-on-the-scale. Type-check green.
- **Contact-form backend live** (hero + contact page). Added `src/app/api/contact/route.ts` — Next.js route handler with Zod schema, posts to `https://api.brevo.com/v3/smtp/email`. Notification email is sent `from contact@korporex.com → to contact@korporex.com` with `replyTo` set to the submitter's email, so clicking "Reply" in Gmail responds directly to the customer. Email body is a clean HTML summary with gold-accent styling matching the brand. Dev fallback: when `BREVO_API_KEY` is missing, submission is logged to the server console and the API returns `{ ok: true, dev: true }` so the UI still completes its success flow. Hero + contact-page forms now show a "Sending…" state and an inline error if the send fails. Type-check, lint, and production build all green (20 routes — `/api/contact` is the new dynamic route). `.env.local.example` updated with a documented `BREVO_API_KEY` entry. Scope: contact forms only — wizard step-7 intake is unchanged in this pass (see updated known-issues entry).
- **Brand assets as SVG** — Created scalable SVG logo + business card files under `public/`:
  - `public/logo/logo-mark.svg` (64×64 K badge)
  - `public/logo/logo-full.svg` (mark + wordmark, light-bg lockup)
  - `public/logo/logo-full-white.svg` (mark + wordmark, dark-bg lockup)
  - `public/business-card/business-card-front.svg` (89×51 mm, navy w/ centered K + tagline)
  - `public/business-card/business-card-back.svg` (89×51 mm, cream w/ `YOUR NAME` / `YOUR TITLE` placeholders + contact)
  - All use exact brand tokens: `#1B4332` forest green, `#C5A35A` gold, `#FAFAF8` cream, Playfair Display serif + Inter sans. Logos are text-based SVG (fonts from next/font on site) — for print, outline fonts to paths in Illustrator/Figma/Inkscape before sending to printer.
  - Navbar unchanged (still uses the inline `<div>`-based KLogo); optional follow-up to swap in `logo-mark.svg` via `next/image`.
  - Exported PNG renders of all five SVGs via Chrome headless (no image-converter dependency added). Outputs in `public/logo/png/` (`logo-mark.png` 512×512, `logo-full.png` / `logo-full-white.png` 1400×320) and `public/business-card/png/` (`business-card-front.png` / `business-card-back.png` 1050×600 — roughly 300 DPI for 89×51 mm cards). Added `/build-tmp` to `.gitignore` for the headless-render scratch folder.
- **Outbound email live.** Generated dedicated Brevo SMTP key `gmail-send-as` (Active). Configured Gmail "Send mail as" for `contact@korporex.com` pointing at `smtp-relay.brevo.com:587` (TLS) with the Brevo-auto-generated SMTP login. Verification email flowed: Gmail → Brevo → recipient DNS → Cloudflare Email Routing → owner's Gmail → clicked verification link → confirmed. Sent a live test from Gmail as `contact@korporex.com` — delivered successfully. `contact@korporex.com` is now bidirectional.
- **Scope decisions**: `korporex.com` is the only domain in play — `korporex.ca` is explicitly **not** being registered at this time. First-phase mailboxes are scoped to `contact@` only; `support@` and `noreply@` are **deferred** to a future phase. Update any suggestion / plan / email-relay config to reflect just `contact@korporex.com`.
- **Email infrastructure — inbound live, outbound in progress.**
  - `korporex.com` registered; DNS managed in Cloudflare (nameservers `doug.ns.cloudflare.com`, `melany.ns.cloudflare.com`).
  - Cloudflare Email Routing configured: `contact@korporex.com` forwards to the owner's personal Gmail — inbound verified working.
  - Outbound path chosen: Brevo SMTP relay (`smtp-relay.brevo.com:587`, TLS) + Gmail "Send mail as", so replies/new mail appear to come from `contact@korporex.com` while using the Gmail UI. Brevo free tier = 300 emails/day, adequate for current form-submission volume.
  - Brevo account provisioned; SMTP credentials visible in dashboard (SMTP username is Brevo's auto-generated `*@smtp-brevo.com` login, not the account email — noted because it's a common setup gotcha). Active SMTP key exists; full key only shown once at creation, so regenerate + save if not archived.
  - Pending before outbound is live: (1) merge Brevo into existing SPF record on `korporex.com` — currently `v=spf1 include:_spf.mx.cloudflare.net ~all`, needs `include:spf.brevo.com` added before `~all`; (2) add DKIM records from Brevo's domain-verification wizard; (3) add Gmail send-as entry pointing at Brevo SMTP and click the verification link (routes back via Cloudflare forwarding); (4) add `_dmarc` TXT record with `p=none` for monitoring.
- **Next**: Wire the contact / hero / wizard forms to a real intake path. Recommended approach — **Brevo Transactional API** from a Next.js API route (`/api/contact`, `/api/incorporate`): reuses the existing Brevo account + domain auth, no new service, stays on the free 300/day tier. Send two emails per submission: (1) notification to `contact@korporex.com` with full payload, (2) auto-reply to the submitter confirming receipt. Store nothing server-side initially — email-as-database is fine for launch volume. Closes both high-sev intake issues. Then Stripe for the wizard and finalize NUANS pass-through pricing.
- **Open verification**: confirm Brevo domain verification (DKIM records in Cloudflare) is complete — needed for reliable deliverability to non-Gmail recipients. 2-min check: send test from Gmail-as-`contact@korporex.com` to a fresh `mail-tester.com` address, target 10/10.

### 2026-04-21
- **Google Maps address autofill is now LIVE in production**. Full setup done end-to-end:
  - Created GCP project; enabled Maps JavaScript API + Places API.
  - Generated browser key, restricted to HTTP referrers (`localhost:3000`, `*.vercel.app`, `korporex.com`, `korporex.ca`) and scoped to Maps JS + Places APIs only.
  - Set `NEXT_PUBLIC_GOOGLE_MAPS_API_KEY` in Vercel across all environments and redeployed.
  - Attached a billing profile to the GCP project — without it, Google returned `BillingNotEnabledMapError` on every request even though the key was valid. Free $200/mo Maps credit covers ~70k autocomplete sessions; real billing only triggers well above current traffic.
  - User verified working: address fields on `/incorporate` return real Google Places suggestions and autofill street/city/region/postal/country on selection.
  - Note: `korporex.com` currently serves a separate WordPress site (unrelated to this Next.js app). DNS cutover to point at Vercel is a future decision — not blocking anything today since the Vercel URL works and the API key's referrer list already includes the domain for whenever it switches over.
- **Wizard upgrade** — Full rewrite of `src/app/incorporate/page.tsx`:
  - Step 3: toggle between **named** and **numbered** corporation (numbered skips NUANS fee).
  - Step 3: added **NAICS industry combobox** (`src/components/NaicsCombobox.tsx` backed by `src/lib/naics.ts` with ~420 curated Canadian NAICS 2022 codes + `searchNaics()` scoring).
  - Step 3: fiscal year end now captures **month + day** (day options derive from the selected month's length).
  - Steps 4, 5, 7: addresses are now **international-friendly** via a shared `AddressFields` subform (country-aware region selector, generic postal/ZIP field).
  - All address inputs wired to **`AddressAutocomplete`** (`src/components/AddressAutocomplete.tsx`) — uses Google Places when `NEXT_PUBLIC_GOOGLE_MAPS_API_KEY` is set, falls back to plain text input when absent. Autofills street/city/region/postal/country from the selected place.
  - Step 6 (registered office) is Canada-only with province locked to ON or BC for provincial incorporations; federal lets the user pick any Canadian province/territory.
  - Step 7 (Review & Pay) now captures **billing name + billing address** and shows a **live tax calculation** based on the billing address (GST/HST/QST for Canadian provinces, 0% international) plus a separate **NUANS fee line** for federal/Ontario named corporations (`NUANS_FEE = 45`, flagged in comments as pass-through — adjust when final pricing confirmed).
  - Step 1 copy rewritten to remove jurisdiction-superiority language (no more "preferred by investors", "best for businesses based in...").
- **Content pass** across marketing pages (home, about, contact, pricing, FAQ, footer, confirmation):
  - Removed placeholder phone number `+1 (888) 000-0000`, fake Toronto office address, and `support@korporex.com` — replaced with `contact@korporex.com` as the single enquiry address.
  - Removed "expert team review" / "Our team reviews your application" phrasing throughout (we're a document-prep & filing platform, not a reviewing team).
  - Removed "no lawyers required" and similar implicit-legal language; positioned as "100% online" instead.
  - Added international-founder positioning ("Canadian and international founders alike") where appropriate.
  - Rewrote federal/provincial explanation in FAQ and home to neutral ("each is a valid route — depends on where you operate, name-protection scope, budget") rather than presenting one as superior.
  - Added a dedicated "Are NUANS fees included?" FAQ entry + made NUANS-fee-not-included explicit in pricing hero copy.
  - Removed "Most Popular" badge from home jurisdiction cards — jurisdiction is not a product tier.
- **New shared components**:
  - `src/components/NaicsCombobox.tsx` — searchable, keyboard-navigable combobox over NAICS codes with sector grouping.
  - `src/components/AddressAutocomplete.tsx` — Google Places-powered input with graceful no-key fallback and structured parsing.
- Verified `npx tsc --noEmit`, `npm run lint`, and `npm run build` — all pass (19 static pages prerendered; wizard route weight 59.4 kB / 156 kB first-load).
- **Next**: Register the Korporex domain (likely `korporex.ca` + `korporex.com`); set up the first professional mailboxes (`contact@`, `support@`, `noreply@`). Configure `NEXT_PUBLIC_GOOGLE_MAPS_API_KEY` in Vercel to enable address autofill. Then wire the contact/hero/wizard forms to a real backend — unblocks the two high-sev form-intake issues. Finalize NUANS pass-through pricing and update `NUANS_FEE` if different from $45. Long-term: add per-article OpenGraph metadata; add search/filter on `/resources` when article count grows.
- Built real Resources article pages. Added `src/app/resources/articles.ts` with six full articles (typed section data, not markdown), dynamic route `src/app/resources/[slug]/page.tsx` with breadcrumb, TOC sidebar, related-articles, and CTA, and updated `src/app/resources/page.tsx` to use real slugs. No outbound competitor links. Resolves placeholder-anchor issue.
- Fixed site metadata in `src/app/layout.tsx` — removed banned "advisory" phrase per content guidelines; replaced with incorporation-focused title/description.
- Deleted duplicate old project folder at `C:\Users\marke\OneDrive\Documents\Korporex Website\`. Verified uncommitted changes were byte-identical to committed state in this folder before deletion. Resolves the low-sev stale-folder issue.
- Verified `npx tsc --noEmit`, `npm run lint`, and `npm run build` — all pass. 6 article routes statically prerendered.
- **Next**: Register the Korporex domain (likely `korporex.ca` + `korporex.com`); set up the first professional mailboxes (recommend `hello@`, `support@`, `noreply@`). Then wire the contact/hero/wizard forms to a real backend — unblocks the two high-sev form-intake issues. Long-term: add per-article OpenGraph metadata; add a search/filter on `/resources` when article count grows.
- Bootstrapped `progress.md` and `known-issues.md` per universal CLAUDE.md guidelines.
- Untracked `.claude/settings.local.json` and added `.claude/` to `.gitignore` — local Claude Code permissions should not be shared (commit `d9a6aad`).
- Added Resources page (`src/app/resources/page.tsx`) with three category tiles and article list; wired into Navbar and Footer (commit `f4c0dcd`).
- Extracted `HeroContactForm` client component for the home-page hero.
- Removed "Home" link from nav — K logo handles home navigation.
- Updated CLAUDE.md: link to universal guidelines at `../CLAUDE.md`, document tracking files, fix `npm lint` → `npm run lint`, note TypeScript strict mode.
- **Next**: Push commits to GitHub (triggers Vercel deploy). Delete old folder `C:\Users\marke\OneDrive\Documents\Korporex Website` once push verified. Replace stub `#` article links on Resources page when content exists.

### 2026-04-20
- Strengthened CLAUDE.md messaging that Korporex is not a law firm (commit `6f33451`).
- Added initial CLAUDE.md with development guidance (commit `16b2961`).
- Upgraded site to full incorporation platform: 8-step wizard, jurisdiction selector, services categories (commit `b4f37fe`).
- Initial Next.js 14 marketing site build with Tailwind, custom color palette, navbar/footer (commit `5e61588`).
